{"generation":0,"content":"--- BEGIN PREVIOUS CAPSULE ---\n(none; this is the first generation)\n--- END PREVIOUS CAPSULE ---\n\n--- BEGIN DURABLE MEMORY CAPSULE ---\n# FreetoShop Clone Stamp Milestone — Durable Memory\n\n## Task State\n- **Goal:** Implement FreetoShop, a local-first browser image editor, milestone by milestone.\n- **Current milestone:** Milestone 1 — Clone Stamp foundation.\n- **Phase:** Initial architecture inspection before implementation.\n- **Working directory:** `C:/Users/disti/benchmark_work/freetoshop_pi_vs_orchid_20260820/arm-b-orchid`\n\n## Repository Structure (established)\n- Monorepo with npm workspaces: `apps/web`, `packages/core-document`, `packages/command-history`, `packages/compositor`, `packages/renderer`, `packages/raster-engine`, `packages/selection-engine`, `packages/filter-engine`, `packages/transform-engine`, `packages/text-engine`, `packages/file-codecs`, `packages/persistence`, `packages/benchmarks`, `packages/pixel-storage`.\n- `apps/web/src/` contains: `App.tsx`, `commands/`, `components/`, `editor/`, `panels/`, `shell/`, `tools/`, `ui/`, `workers/`, `workspace/`.\n- `tests/e2e/` exists for browser-level tests.\n- Key docs: `ARCHITECTURE.md`, `DOCUMENT_MODEL.md`, `IMPLEMENTATION_ROADMAP.md`, `TILED_RENDERING.md`, `UI_WORKSPACE_ARCHITECTURE.md`, `WORKER_OPERATIONS.md`, `TESTING.md`.\n\n## Architecture Facts (from ARCHITECTURE.md, partial read)\n- **apps/web:** owns interaction, accessibility, PWA, view state. Delegates to `editor/filterOperations`, `editor/types`, `editor/model`, `components/`. `workers/` is the browser-process boundary for long-running local ops.\n- **core-document:** owns serializable editor state, layer ordering, stable IDs, invertible affine layer transforms, validated edits, editable shape geometry/styles, vector paths, text content/styles, ordered typed layer effects, smart-object source trees, smart-filter state, nondestructive canvas geometry. Pixel layers own immutable sparse `TiledPixelStore`; raster masks use `TiledCoverageStore`; copy-on-write patches.\n- **command-history:** applies undoable commands, owns undo/redo history.\n- **compositor:** DOM-free premultiplied-alpha source-over, 20 separable + 6 RGB-vector blend modes, immutable/in-place same-sized composition.\n- **renderer:** document + viewport → Canvas 2D pixels; must not mutate document; rasterizes affine pixel/shape/text layers, resolves clipping, group isolation, masks over pass-through/isolated groups, composites smart filters.\n\n## Tooling/Interaction Model (from toolRegistry.ts, partial)\n- Tool registry exists at `apps/web/src/tools/toolRegistry.ts` and `toolRegistry.test.ts`.\n- Existing tools include Brush and Eraser workflows that must be preserved.\n- Toolbar/tool-option architecture exists; Clone Stamp should fit the retouch/painting toolbar family.\n\n## Implementation Constraints (from user goal)\n- Clone Stamp must support: Alt/Option-click to set source; visible source state; aligned mode (preserves source-dest offset across gesture/segments); unaligned mode (restarts from source origin); correct transformed-layer coordinate mapping; respects active selection (clips writes); respects mask/edit-target model; one atomic undoable transaction per gesture; persists/survives save-reopen/recovery; missing source handled visibly and safely.\n- Must use explicit testable data model for clone source/aligned-unaligned/coordinate relationship — not implicit UI-handler state.\n- Reuse tiled stores and coordinate helpers; do not materialize entire canvas/layer for a clone op.\n- Add focused unit tests: source/dest pixel correctness, multi-segment aligned strokes, unaligned restart, transformed layers, selection clipping, masks/edit targets, tile-boundary behavior. Add browser workflow verifying undo/redo + persistence/recovery.\n- Do not regress existing Brush/Eraser behavior.\n\n## Cross-Cutting Acceptance Questions (must answer per feature)\n1. Active edit target (document/layer/mask/channel)?\n2. Coordinate space for pointer/source/selection/layer/document?\n3. Which tiles/bounded region are read, allocated, rendered, written?\n4. Behavior when selection empty, source missing, layer transformed, resource not hydrated, optional backend missing?\n5. Exact command/history boundary for one user action; undo/redo without changing unrelated state?\n6. What is persisted, how reopened, how recovered after interruption?\n\n## Notes / Next Steps\n- Continue reading ARCHITECTURE.md, DOCUMENT_MODEL.md, toolRegistry.ts, and related editor/model files to identify the correct extension points for Clone Stamp (command/history, tiled stores, coordinate helpers, brush/raster workflow, selection/mask handling).\n- Identify existing brush/raster workflow and command definitions to reuse for clone sampling + destination writes.\n- Plan explicit clone-source data model and aligned/unaligned semantics before writing code.\n- Keep repository buildable; prefer small complete slice over decorative breadth.\n--- END DURABLE MEMORY CAPSULE ---","content_hash":"7356bb2525892a03662ec34beb6de7b4f5313e05555194b85a77b906fdeb713f","base_capsule_hash":null,"source_refs":["replay_evt_000001","replay_evt_000002::span::000000","replay_evt_000002::span::000001","replay_evt_000002::span::000002","replay_evt_000002::span::000003","replay_evt_000002::span::000004","replay_evt_000003","replay_evt_000004","replay_evt_000005","replay_evt_000006","replay_evt_000007::span::000000","replay_evt_000007::span::000001","replay_evt_000008","replay_evt_000009","replay_evt_000010::span::000000"],"covered_end_sequence":10}
{"generation":1,"content":"# FreetoShop Clone Stamp Milestone — Durable Memory\n\n## Task State\n- **Goal:** Implement FreetoShop, a local-first browser image editor, milestone by milestone.\n- **Current milestone:** Milestone 1 — Clone Stamp foundation.\n- **Phase:** Initial architecture inspection before implementation.\n- **Working directory:** `C:/Users/disti/benchmark_work/freetoshop_pi_vs_orchid_20260820/arm-b-orchid`\n\n## Repository Structure (established)\n- Monorepo with npm workspaces: `apps/web`, `packages/core-document`, `packages/command-history`, `packages/compositor`, `packages/renderer`, `packages/raster-engine`, `packages/selection-engine`, `packages/filter-engine`, `packages/transform-engine`, `packages/text-engine`, `packages/file-codecs`, `packages/persistence`, `packages/benchmarks`, `packages/pixel-storage`.\n- `apps/web/src/` contains: `App.tsx`, `commands/`, `components/`, `editor/`, `panels/`, `shell/`, `tools/`, `ui/`, `workers/`, `workspace/`.\n- `tests/e2e/` exists for browser-level tests.\n- Key docs: `ARCHITECTURE.md`, `DOCUMENT_MODEL.md`, `IMPLEMENTATION_ROADMAP.md`, `TILED_RENDERING.md`, `UI_WORKSPACE_ARCHITECTURE.md`, `WORKER_OPERATIONS.md`, `TESTING.md`.\n\n## Architecture Facts (from ARCHITECTURE.md, DOCUMENT_MODEL.md, raster-engine/index.ts, toolRegistry.ts)\n- **apps/web:** owns interaction, accessibility, PWA, view state. Delegates to `editor/filterOperations`, `editor/types`, `editor/model`, `components/`. `workers/` is the browser-process boundary for long-running local ops.\n- **core-document:** owns serializable editor state, layer ordering, stable IDs, invertible affine layer transforms, validated edits, editable shape geometry/styles, vector paths, text content/styles, ordered typed layer effects, smart-object source trees, smart-filter state, nondestructive canvas geometry. Pixel layers own immutable sparse `TiledPixelStore`; raster masks use `TiledCoverageStore`; copy-on-write patches.\n- **command-history:** applies undoable commands, owns undo/redo history.\n- **compositor:** DOM-free premultiplied-alpha source-over, 20 separable + 6 RGB-vector blend modes, immutable/in-place same-sized composition.\n- **renderer:** document + viewport → Canvas 2D pixels; must not mutate document; rasterizes affine pixel/shape/text layers, resolves clipping, group isolation, masks over pass-through/isolated groups, composites smart filters.\n- **raster-engine:** owns deterministic, DOM-free pixel and mask operations. Brush, eraser, and adjustments consume document-space selection coverage and inverse-map affine coordinates, and return minimal layer-local RGBA or one-channel coverage patches. Same mask stroke path edits pixel, shape, text, and smart-object masks through nested world transforms and group masks directly in document space. Image Size uses nearest-neighbor and bilinear pixel-center resamplers for pixel layers, layer masks, selections, and channels; bilinear RGBA interpolation is performed in premultiplied alpha.\n  - Exports: `BrushPoint`, `BrushOptions` (mode/source-over|erase, size, hardness, opacity, color), `MaskBrushOptions` (mode/reveal|conceal, size, hardness, opacity), `CoveragePatch`, `PixelPatch`, `ResamplingMethod` (nearest|bilinear), `ResizeImageOptions`.\n  - Has `sourceCoordinate(destination, sourceSize, destinationSize)` and `clampIndex` helpers; `resampleCoverage`, `resampleCoverageStore`, `resamplePixels`, `resamplePixelStore` for tiled resampling.\n- **selection-engine:** creates and refines owned, canvas-sized 8-bit coverage masks. Owns supersampled ellipse/polygon rasterization, RMS-color Magic Wand matching, deterministic mask morphology; does not depend on DOM or renderer.\n- **filter-engine:** returns bounded immutable pixel patches for destructive color and Gaussian operations. Maps document-space selection coverage through supplied layer world transform and leaves command/history policy to the caller. Renderer reuses its Gaussian implementation for nondestructive smart-object intermediate surfaces.\n- **transform-engine:** owns DOM-free transform snapshots, local/document handle geometry, nested-parent-aware scaling, rotation, translation, singularity protection. Web app owns pointer capture, previews, accessible controls. Web app projects editable path anchors and cubic controls through nested world transforms; core-document remains canonical geometry/matrix authority.\n- **text-engine:** lazily loads HarfBuzz WebAssembly, owns bounded local-font registration, OpenType substitution/positioning, direction inference, line layout, positioned glyph outlines. Renderer consumes outlines; system fonts without available binaries use explicit Canvas fallback.\n- **file-codecs:** translates PNG/JPEG bytes to/from document model; bounded import-only PSD translation through `ag-psd`. PSD parsing validates binary header before decode and returns explicit compatibility report; does not leak `ag-psd` objects into canonical state. Imported data is untrusted.\n- **persistence:** saves/restores supported state in IndexedDB. No server required; document contents should not leave device implicitly. Owns versioned `.freetoshop` binary container, schema migration, SHA-256 integrity verification, canonical native-file decoding.\n- **benchmarks:** builds versioned synthetic documents and enforces broad CI-safe ceilings for compositor time, brush latency, typed-array storage amplification. Production packages must never depend on it.\n- **pixel-storage:** owns sparse immutable tile stores, copy-on-write pixel patches, sparse one-channel coverage stores, contiguous and tiled `PixelSource` adapters, owned region reads, chunk iteration, validated deterministic worker/persistence snapshots, clipped/coalesced dirty-region accumulation. No dependency on document model or renderer.\n\n## Document Model Facts (from DOCUMENT_MODEL.md)\n- Pixel layers canonically own immutable sparse `TiledPixelStore` resources. Pixel patches use copy-on-write tiles, so history versions may share unchanged tiles without exposing mutable RGBA buffers. Contiguous materialization is an explicit compatibility operation at legacy filter, resize, codec, export, or version-1 persistence boundaries.\n- Raster masks, selections, and saved alpha channels own immutable sparse one-channel `TiledCoverageStore` resources only; no dual contiguous ownership field. Coverage stores carry implicit 8-bit default plus sparse deviation tiles, so partial selection and raster-mask complements remain sparse through inversion, crop, resize, worker transport, native persistence, and selection refinement.\n- Affine selection-to-raster-mask baking reads only active selection tiles and writes a bounded sparse destination rather than allocating full layer-sized coverage buffer. Sparse selection combine reconciles different source tile sizes on one bounded output grid without materializing either full canvas.\n- Cold-composite worker keeps isolated registry of immutable tiled resources. Version-1 task envelopes upload each pixel/coverage snapshot once per store and snapshot scope, then use resource references for later metadata-only document revisions. Version-2 delta envelopes derive next immutable store from retained predecessor, transferring only COW tiles whose buffer identity changed and listing removed tile keys without payload. A task carries its live resource set so superseded stores are evicted on both sides. Abort terminates worker and discards namespace; never reuses references or delta bases across cancellation. Filter, resize, and native-export operations share stable document resource slots in one sequential session. Resize, PSD-import, and native-import results use collision-safe request-scoped resource IDs; worker retains owned clones before transferring buffers, client adopts hydrated stores under same IDs for zero-upload follow-up tasks.\n- Document model is canonical, serializable description of an edit. Document has version, canvas dimensions, ordered layers, stable IDs. Renderable layers refer to image resources rather than DOM or canvas objects. Viewport zoom, pan, selection chrome, dialogs, hover state are transient UI state. Full-resolution selection mask is document state because raster commands, masks, and adjustments consume its 8-bit coverage.\n- Free Transform snapshots, active handles, pointer positions, live previews are transient; only final validated affine matrix enters document history and persistence. Crop drafts are likewise transient; applied crop is represented by new canvas dimensions, translated root transforms, and remapped canvas-sized coverage buffers.\n- Invariants: dimensions, coordinates, opacity, resource sizes are finite and bounded; layer IDs unique within document, layer order deterministic; unsupported layer kinds or enum values fail validation rather than being executed or silently reinterpreted; edits expressed as commands or equivalent pure state transitions so they can be tested and later support undo/redo; saved documents contain data, never executable JavaScript; every layer owns invertible Canvas-style affine matrix (a through f); nested world transforms compose parent × child, supporting translation, rotation, scale, reflection, shear without rewriting source pixels; selection is either null (all pixels) or owned canvas-sized sparse `TiledCoverageStore`; rectangles write only dirty patches; arbitrary masks can replace, add, subtract, or intersect coverage; add uses coverage union; subtract and intersect multiply soft coverage rather than thresholding it; saved alpha channels use same sparse ownership contract; ellipse and lasso results retain 8-bit antialiased edges; Magic Wand produces binary coverage from composite RGBA byte distance; feather, expand, contract, smooth, border produce new owned masks and enter history as one selection command; derived outline geometry and live gesture previews remain view state; selection-engine canvases limited to 32,768 pixels per axis and 100,000,000 pixels total; UI limits refinement radii to 100 pixels; feather is zero-padded square box filter, expand/contract are square maximum/minimum morphology, smooth is square median, border is expanded coverage minus contracted coverage; these are deterministic foundations, not professional Select and Mask/refine-edge model; pixel, shape, text, smart-object, group, and adjustment raster masks own immutable sparse 8-bit coverage stores; their enabled state and normalized density are nondestructive layer properties; painting a structured layer mask never rasterizes its source; mask painting applies immutable bounded coverage patches and never rewrites owning layer's RGBA buffer; pixel/mask targeting is transient UI state; every layer owns validated blend mode, independent opacity/fill opacity, clipping flag, transparency/pixel/position/all lock set; pixel locks reject raster and mask mutation; transparency locks preserve existing alpha during pixel patches; position locks reject affine edits; all locks reject property edits; groups additionally choose pass-through or isolated compositing; adjustment layers own typed Brightness/Contrast, Levels, Hue/Saturation, or Invert payload; optional raster mask is canvas-sized document-space coverage; parameters and masks remain editable and never rewrite underlying pixel layers; shape layers own validated rectangle, ellipse, or arbitrary path geometry and editable fill, stroke, stroke width, and rectangle corner radius; paths own ordered move, line, cubic Bézier, and close commands with finite anchors and controls, bounded to 10,000 commands; text layers own multiline content, fixed layout box, validated family, size, weight, italic, line-height, alignment, color style data; both remain structured rather than storing cached pixels and may carry layer-local raster mask; renderable layers own ordered list of validated color-overlay, outside-stroke, and drop-shadow effects; effect enabled state, opacity, color, blend mode, size, offsets, blur are canonical editable data; source pixels and structured payloads are never baked when an effect changes.\n\n## Tooling/Interaction Model (from toolRegistry.ts, editor/types.ts)\n- Tool registry exists at `apps/web/src/tools/toolRegistry.ts` and `toolRegistry.test.ts`.\n- Existing tools include Brush and Eraser workflows that must be preserved.\n- Toolbar/tool-option architecture exists; Clone Stamp should fit the retouch/painting toolbar family.\n- `Tool` type currently: `'pan' | 'move' | 'transform' | 'crop' | 'select' | 'ellipse' | 'lasso' | 'wand' | 'pen' | 'brush' | 'eraser'`.\n- `EditTarget` type: `'pixels' | 'mask'`.\n- `Gesture` union includes paint gesture: `{ kind: 'paint'; points: BrushPoint[]; layerId: string; target: EditTarget }`.\n- `BrushPoint`: document-space x, y, optional normalized tablet pressure (defaults to 1).\n- `SmartObjectEditSession`: `{ parentHistory, layerId, sourceId, sourceName, parentViewport }`.\n- `DocumentPoint`, `DocumentRectangle`, `ViewportTransform`, `LayerTransform`, `FreeTransformSnapshot`, `TransformHandleId`, `ShapeGeometry`, `PathHandleRole` types available.\n\n## Implementation Constraints (from user goal)\n- Clone Stamp must support: Alt/Option-click to set source; visible source state; aligned mode (preserves source-dest offset across gesture/segments); unaligned mode (restarts from source origin); correct transformed-layer coordinate mapping; respects active selection (clips writes); respects mask/edit-target model; one atomic undoable transaction per gesture; persists/survives save-reopen/recovery; missing source handled visibly and safely.\n- Must use explicit testable data model for clone source/aligned-unaligned/coordinate relationship — not implicit UI-handler state.\n- Reuse tiled stores and coordinate helpers; do not materialize entire canvas/layer for a clone op.\n- Add focused unit tests: source/dest pixel correctness, multi-segment aligned strokes, unaligned restart, transformed layers, selection clipping, masks/edit targets, tile-boundary behavior. Add browser workflow verifying undo/redo + persistence/recovery.\n- Do not regress existing Brush/Eraser behavior.\n\n## Cross-Cutting Acceptance Questions (must answer per feature)\n1. Active edit target (document/layer/mask/channel)?\n2. Coordinate space for pointer/source/selection/layer/document?\n3. Which tiles/bounded region are read, allocated, rendered, written?\n4. Behavior when selection empty, source missing, layer transformed, resource not hydrated, optional backend missing?\n5. Exact command/history boundary for one user action; undo/redo without changing unrelated state?\n6. What is persisted, how reopened, how recovered after interruption?\n\n## Notes / Next Steps\n- Continue reading ARCHITECTURE.md, DOCUMENT_MODEL.md, toolRegistry.ts, and related editor/model files to identify the correct extension points for Clone Stamp (command/history, tiled stores, coordinate helpers, brush/raster workflow, selection/mask handling).\n- Identify existing brush/raster workflow and command definitions to reuse for clone sampling + destination writes.\n- Plan explicit clone-source data model and aligned/unaligned semantics before writing code.\n- Keep repository buildable; prefer small complete slice over decorative breadth.\n- **Immediate next reads:** `editor/types.ts` full content (already partially read), existing command definitions in `apps/web/src/commands/`, brush/eraser tool implementations, `core-document` layer/patch APIs, `pixel-storage` tile read/write APIs, `command-history` command contract.","content_hash":"8d10f16b27afa90d0843f7e286970156d5cbbcd983800c453f1270d6443553c2","base_capsule_hash":"7356bb2525892a03662ec34beb6de7b4f5313e05555194b85a77b906fdeb713f","source_refs":["replay_evt_000010::span::000001","replay_evt_000010::span::000002","replay_evt_000011::span::000000","replay_evt_000011::span::000001","replay_evt_000011::span::000002","replay_evt_000012","replay_evt_000013","replay_evt_000014","replay_evt_000015","replay_evt_000016::span::000000","replay_evt_000016::span::000001","replay_evt_000016::span::000002","replay_evt_000016::span::000003","replay_evt_000016::span::000004"],"covered_end_sequence":16}
{"generation":2,"content":"# FreetoShop Clone Stamp Milestone — Durable Memory\n\n## Task State\n- **Goal:** Implement FreetoShop, a local-first browser image editor, milestone by milestone.\n- **Current milestone:** Milestone 1 — Clone Stamp foundation.\n- **Phase:** Initial architecture inspection before implementation.\n- **Working directory:** `C:/Users/disti/benchmark_work/freetoshop_pi_vs_orchid_20260820/arm-b-orchid`\n\n## Repository Structure (established)\n- Monorepo with npm workspaces: `apps/web`, `packages/core-document`, `packages/command-history`, `packages/compositor`, `packages/renderer`, `packages/raster-engine`, `packages/selection-engine`, `packages/filter-engine`, `packages/transform-engine`, `packages/text-engine`, `packages/file-codecs`, `packages/persistence`, `packages/benchmarks`, `packages/pixel-storage`.\n- `apps/web/src/` contains: `App.tsx`, `commands/`, `components/`, `editor/`, `panels/`, `shell/`, `tools/`, `ui/`, `workers/`, `workspace/`.\n- `tests/e2e/` exists for browser-level tests.\n- Key docs: `ARCHITECTURE.md`, `DOCUMENT_MODEL.md`, `IMPLEMENTATION_ROADMAP.md`, `TILED_RENDERING.md`, `UI_WORKSPACE_ARCHITECTURE.md`, `WORKER_OPERATIONS.md`, `TESTING.md`.\n\n## Architecture Facts (from ARCHITECTURE.md, DOCUMENT_MODEL.md, raster-engine/index.ts, toolRegistry.ts)\n- **apps/web:** owns interaction, accessibility, PWA, view state. Delegates to `editor/filterOperations`, `editor/types`, `editor/model`, `components/`. `workers/` is the browser-process boundary for long-running local ops.\n- **core-document:** owns serializable editor state, layer ordering, stable IDs, invertible affine layer transforms, validated edits, editable shape geometry/styles, vector paths, text content/styles, ordered typed layer effects, smart-object source trees, smart-filter state, nondestructive canvas geometry. Pixel layers own immutable sparse `TiledPixelStore`; raster masks use `TiledCoverageStore`; copy-on-write patches.\n- **command-history:** applies undoable commands, owns undo/redo history.\n- **compositor:** DOM-free premultiplied-alpha source-over, 20 separable + 6 RGB-vector blend modes, immutable/in-place same-sized composition.\n- **renderer:** document + viewport → Canvas 2D pixels; must not mutate document; rasterizes affine pixel/shape/text layers, resolves clipping, group isolation, masks over pass-through/isolated groups, composites smart filters.\n- **raster-engine:** owns deterministic, DOM-free pixel and mask operations. Brush, eraser, and adjustments consume document-space selection coverage and inverse-map affine coordinates, and return minimal layer-local RGBA or one-channel coverage patches. Same mask stroke path edits pixel, shape, text, and smart-object masks through nested world transforms and group masks directly in document space. Image Size uses nearest-neighbor and bilinear pixel-center resamplers for pixel layers, layer masks, selections, and channels; bilinear RGBA interpolation is performed in premultiplied alpha.\n  - Exports: `BrushPoint`, `BrushOptions` (mode/source-over|erase, size, hardness, opacity, color), `MaskBrushOptions` (mode/reveal|conceal, size, hardness, opacity), `CoveragePatch`, `PixelPatch`, `ResamplingMethod` (nearest|bilinear), `ResizeImageOptions`.\n  - Has `sourceCoordinate(destination, sourceSize, destinationSize)` and `clampIndex` helpers; `resampleCoverage`, `resampleCoverageStore`, `resamplePixels`, `resamplePixelStore` for tiled resampling.\n  - **Key implementation patterns observed in raster-engine source:**\n    - `paintStroke(layer, points, options, selection?, worldTransform?)` returns `PixelPatch`; validates layer/transform/selection; builds interpolated dabs from points; computes union of local dab bounds only (no full-layer densify); reads region from `layer.pixelStore.read(...)`; applies per-pixel falloff × opacity × pressure × selection coverage; erase multiplies alpha by (1-coverage), source-over uses `compositeSourceOver`; tracks `changedBounds` and returns `makePatchFromRegion(...)`.\n    - `paintMaskStroke(mask, layerTransform, points, options, selection?)` returns `CoveragePatch`; similar dab/interpolation/bounds pattern for one-channel coverage; reveal target 255, conceal target 0; blends toward target by strength.\n    - `adjustBrightness(layer, amount, selection?, worldTransform?)` returns `PixelPatch`; full-layer path when selection null (entire layer work bounds), sparse path when selection present (densifies only inverse-mapped selection bounds via `layerBoundsFromDocumentBounds`); blends RGB by coverage.\n    - `adjustBrightnessPixelStore(layer, amount, selection?, worldTransform?)` writes directly into immutable canonical tile store one destination tile at a time, avoiding rectangular `PixelPatch` allocation.\n    - `selectionCoverage(selection, documentX, documentY, regionBuffer?, region?)` returns 1 for null selection; samples floor coords; supports region-buffered reads for batch queries; falls back to single-pixel sparse `coverageStore.read({x,y,1,1})`.\n    - `readSelectionRegion(selection, dabs, radius)` computes union of `documentDabBounds` across dabs, reads that rectangular region from coverage store once.\n    - `localDabBounds(transform, inverse, documentX, documentY, radius, width, height)` projects document-space circle into local-space ellipse using inverse linear row lengths `Math.hypot(inverse.a, inverse.c)` and `Math.hypot(inverse.b, inverse.d)`; returns clamped axis-aligned bounds.\n    - `layerBoundsFromDocumentBounds(documentBounds, inverse, layerWidth, layerHeight)` transforms 4 corners through inverse, expands by 1px for soft-edge aliasing, returns null if outside layer.\n    - `compositeSourceOver(pixels, offset, color, coverage)` does premultiplied source-over with alpha recompute.\n    - `dabCoverage(distance, radius, hardness)` soft falloff.\n    - `multiplyTransforms`, `scalePathCommand`, `resizeLayer`, `resizeImage`, `estimateLayerResizeBytes`, `validateOutputSize`, `validateLayer`, `validateSelection`, `validateRasterMask`, `validateTransform`, `transformPoint`, `invertTransform`, `unionBounds`, `documentDabBounds`, `makePatchFromRegion`, `makeCoveragePatchFromRegion`, `include`, `emptyPatch`, `emptyCoveragePatch` all present.\n- **selection-engine:** creates and refines owned, canvas-sized 8-bit coverage masks. Owns supersampled ellipse/polygon rasterization, RMS-color Magic Wand matching, deterministic mask morphology; does not depend on DOM or renderer.\n- **filter-engine:** returns bounded immutable pixel patches for destructive color and Gaussian operations. Maps document-space selection coverage through supplied layer world transform and leaves command/history policy to the caller. Renderer reuses its Gaussian implementation for nondestructive smart-object intermediate surfaces.\n- **transform-engine:** owns DOM-free transform snapshots, local/document handle geometry, nested-parent-aware scaling, rotation, translation, singularity protection. Web app owns pointer capture, previews, accessible controls. Web app projects editable path anchors and cubic controls through nested world transforms; core-document remains canonical geometry/matrix authority.\n- **text-engine:** lazily loads HarfBuzz WebAssembly, owns bounded local-font registration, OpenType substitution/positioning, direction inference, line layout, positioned glyph outlines. Renderer consumes outlines; system fonts without available binaries use explicit Canvas fallback.\n- **file-codecs:** translates PNG/JPEG bytes to/from document model; bounded import-only PSD translation through `ag-psd`. PSD parsing validates binary header before decode and returns explicit compatibility report; does not leak `ag-psd` objects into canonical state. Imported data is untrusted.\n- **persistence:** saves/restores supported state in IndexedDB. No server required; document contents should not leave device implicitly. Owns versioned `.freetoshop` binary container, schema migration, SHA-256 integrity verification, canonical native-file decoding.\n- **benchmarks:** builds versioned synthetic documents and enforces broad CI-safe ceilings for compositor time, brush latency, typed-array storage amplification. Production packages must never depend on it.\n- **pixel-storage:** owns sparse immutable tile stores, copy-on-write pixel patches, sparse one-channel coverage stores, contiguous and tiled `PixelSource` adapters, owned region reads, chunk iteration, validated deterministic worker/persistence snapshots, clipped/coalesced dirty-region accumulation. No dependency on document model or renderer.\n\n## Document Model Facts (from DOCUMENT_MODEL.md)\n- Pixel layers canonically own immutable sparse `TiledPixelStore` resources. Pixel patches use copy-on-write tiles, so history versions may share unchanged tiles without exposing mutable RGBA buffers. Contiguous materialization is an explicit compatibility operation at legacy filter, resize, codec, export, or version-1 persistence boundaries.\n- Raster masks, selections, and saved alpha channels own immutable sparse one-channel `TiledCoverageStore` resources only; no dual contiguous ownership field. Coverage stores carry implicit 8-bit default plus sparse deviation tiles, so partial selection and raster-mask complements remain sparse through inversion, crop, resize, worker transport, native persistence, and selection refinement.\n- Affine selection-to-raster-mask baking reads only active selection tiles and writes a bounded sparse destination rather than allocating full layer-sized coverage buffer. Sparse selection combine reconciles different source tile sizes on one bounded output grid without materializing either full canvas.\n- Cold-composite worker keeps isolated registry of immutable tiled resources. Version-1 task envelopes upload each pixel/coverage snapshot once per store and snapshot scope, then use resource references for later metadata-only document revisions. Version-2 delta envelopes derive next immutable store from retained predecessor, transferring only COW tiles whose buffer identity changed and listing removed tile keys without payload. A task carries its live resource set so superseded stores are evicted on both sides. Abort terminates worker and discards namespace; never reuses references or delta bases across cancellation. Filter, resize, and native-export operations share stable document resource slots in one sequential session. Resize, PSD-import, and native-import results use collision-safe request-scoped resource IDs; worker retains owned clones before transferring buffers, client adopts hydrated stores under same IDs for zero-upload follow-up tasks.\n- Document model is canonical, serializable description of an edit. Document has version, canvas dimensions, ordered layers, stable IDs. Renderable layers refer to image resources rather than DOM or canvas objects. Viewport zoom, pan, selection chrome, dialogs, hover state are transient UI state. Full-resolution selection mask is document state because raster commands, masks, and adjustments consume its 8-bit coverage.\n- Free Transform snapshots, active handles, pointer positions, live previews are transient; only final validated affine matrix enters document history and persistence. Crop drafts are likewise transient; applied crop is represented by new canvas dimensions, translated root transforms, and remapped canvas-sized coverage buffers.\n- Invariants: dimensions, coordinates, opacity, resource sizes are finite and bounded; layer IDs unique within document, layer order deterministic; unsupported layer kinds or enum values fail validation rather than being executed or silently reinterpreted; edits expressed as commands or equivalent pure state transitions so they can be tested and later support undo/redo; saved documents contain data, never executable JavaScript; every layer owns invertible Canvas-style affine matrix (a through f); nested world transforms compose parent × child, supporting translation, rotation, scale, reflection, shear without rewriting source pixels; selection is either null (all pixels) or owned canvas-sized sparse `TiledCoverageStore`; rectangles write only dirty patches; arbitrary masks can replace, add, subtract, or intersect coverage; add uses coverage union; subtract and intersect multiply soft coverage rather than thresholding it; saved alpha channels use same sparse ownership contract; ellipse and lasso results retain 8-bit antialiased edges; Magic Wand produces binary coverage from composite RGBA byte distance; feather, expand, contract, smooth, border produce new owned masks and enter history as one selection command; derived outline geometry and live gesture previews remain view state; selection-engine canvases limited to 32,768 pixels per axis and 100,000,000 pixels total; UI limits refinement radii to 100 pixels; feather is zero-padded square box filter, expand/contract are square maximum/minimum morphology, smooth is square median, border is expanded coverage minus contracted coverage; these are deterministic foundations, not professional Select and Mask/refine-edge model; pixel, shape, text, smart-object, group, and adjustment raster masks own immutable sparse 8-bit coverage stores; their enabled state and normalized density are nondestructive layer properties; painting a structured layer mask never rasterizes its source; mask painting applies immutable bounded coverage patches and never rewrites owning layer's RGBA buffer; pixel/mask targeting is transient UI state; every layer owns validated blend mode, independent opacity/fill opacity, clipping flag, transparency/pixel/position/all lock set; pixel locks reject raster and mask mutation; transparency locks preserve existing alpha during pixel patches; position locks reject affine edits; all locks reject property edits; groups additionally choose pass-through or isolated compositing; adjustment layers own typed Brightness/Contrast, Levels, Hue/Saturation, or Invert payload; optional raster mask is canvas-sized document-space coverage; parameters and masks remain editable and never rewrite underlying pixel layers; shape layers own validated rectangle, ellipse, or arbitrary path geometry and editable fill, stroke, stroke width, and rectangle corner radius; paths own ordered move, line, cubic Bézier, and close commands with finite anchors and controls, bounded to 10,000 commands; text layers own multiline content, fixed layout box, validated family, size, weight, italic, line-height, alignment, color style data; both remain structured rather than storing cached pixels and may carry layer-local raster mask; renderable layers own ordered list of validated color-overlay, outside-stroke, and drop-shadow effects; effect enabled state, opacity, color, blend mode, size, offsets, blur are canonical editable data; source pixels and structured payloads are never baked when an effect changes.\n\n## Tooling/Interaction Model (from toolRegistry.ts, editor/types.ts)\n- Tool registry exists at `apps/web/src/tools/toolRegistry.ts` and `toolRegistry.test.ts`.\n- Existing tools include Brush and Eraser workflows that must be preserved.\n- Toolbar/tool-option architecture exists; Clone Stamp should fit the retouch/painting toolbar family.\n- `Tool` type currently: `'pan' | 'move' | 'transform' | 'crop' | 'select' | 'ellipse' | 'lasso' | 'wand' | 'pen' | 'brush' | 'eraser'`.\n- `EditTarget` type: `'pixels' | 'mask'`.\n- `Gesture` union includes paint gesture: `{ kind: 'paint'; points: BrushPoint[]; layerId: string; target: EditTarget }`.\n- `BrushPoint`: document-space x, y, optional normalized tablet pressure (defaults to 1).\n- `SmartObjectEditSession`: `{ parentHistory, layerId, sourceId, sourceName, parentViewport }`.\n- `DocumentPoint`, `DocumentRectangle`, `ViewportTransform`, `LayerTransform`, `FreeTransformSnapshot`, `TransformHandleId`, `ShapeGeometry`, `PathHandleRole` types available.\n\n## Implementation Constraints (from user goal)\n- Clone Stamp must support: Alt/Option-click to set source; visible source state; aligned mode (preserves source-dest offset across gesture/segments); unaligned mode (restarts from source origin); correct transformed-layer coordinate mapping; respects active selection (clips writes); respects mask/edit-target model; one atomic undoable transaction per gesture; persists/survives save-reopen/recovery; missing source handled visibly and safely.\n- Must use explicit testable data model for clone source/aligned-unaligned/coordinate relationship — not implicit UI-handler state.\n- Reuse tiled stores and coordinate helpers; do not materialize entire canvas/layer for a clone op.\n- Add focused unit tests: source/dest pixel correctness, multi-segment aligned strokes, unaligned restart, transformed layers, selection clipping, masks/edit targets, tile-boundary behavior. Add browser workflow verifying undo/redo + persistence/recovery.\n- Do not regress existing Brush/Eraser behavior.\n\n## Cross-Cutting Acceptance Questions (must answer per feature)\n1. Active edit target (document/layer/mask/channel)?\n2. Coordinate space for pointer/source/selection/layer/document?\n3. Which tiles/bounded region are read, allocated, rendered, written?\n4. Behavior when selection empty, source missing, layer transformed, resource not hydrated, optional backend missing?\n5. Exact command/history boundary for one user action; undo/redo without changing unrelated state?\n6. What is persisted, how reopened, how recovered after interruption?\n\n## Notes / Next Steps\n- Continue reading ARCHITECTURE.md, DOCUMENT_MODEL.md, toolRegistry.ts, and related editor/model files to identify the correct extension points for Clone Stamp (command/history, tiled stores, coordinate helpers, brush/raster workflow, selection/mask handling).\n- Identify existing brush/raster workflow and command definitions to reuse for clone sampling + destination writes.\n- Plan explicit clone-source data model and aligned/unaligned semantics before writing code.\n- Keep repository buildable; prefer small complete slice over decorative breadth.\n- **Immediate next reads:** `editor/types.ts` full content (already partially read), existing command definitions in `apps/web/src/commands/`, brush/eraser tool implementations, `core-document` layer/patch APIs, `pixel-storage` tile read/write APIs, `command-history` command contract.\n- **Recent reads (raster-engine/src/index.ts spans 5-16):** Confirmed detailed brush/mask/adjustment implementation patterns: `paintStroke`, `paintMaskStroke`, `adjustBrightness`, `adjustBrightnessPixelStore`, `selectionCoverage`, `readSelectionRegion`, `localDabBounds`, `layerBoundsFromDocumentBounds`, `compositeSourceOver`, `dabCoverage`, `multiplyTransforms`, `scalePathCommand`, `resizeLayer`, `resizeImage`, `estimateLayerResizeBytes`, `validateOutputSize`, `validateLayer`, `validateSelection`, `validateRasterMask`, `validateTransform`, `transformPoint`, `invertTransform`, `unionBounds`, `documentDabBounds`, `makePatchFromRegion`, `makeCoveragePatchFromRegion`, `include`, `emptyPatch`, `emptyCoveragePatch`. These are the reusable primitives for clone stamp sampling + destination writes.\n- **Next planned reads:** `packages/core-document/src/index.ts` (layer/patch APIs, document model), `packages/command-history/src/index.ts` (command contract), `apps/web/src/commands/` (existing command definitions), brush/eraser tool implementations, `pixel-storage` tile read/write APIs.","content_hash":"db580def0caf8f926b50e5e8e5f69c72c4400b9cb0acba89385061351001eb2f","base_capsule_hash":"8d10f16b27afa90d0843f7e286970156d5cbbcd983800c453f1270d6443553c2","source_refs":["replay_evt_000016::span::000005","replay_evt_000016::span::000006","replay_evt_000016::span::000007","replay_evt_000016::span::000008","replay_evt_000016::span::000009","replay_evt_000016::span::000010","replay_evt_000016::span::000011","replay_evt_000016::span::000012","replay_evt_000016::span::000013","replay_evt_000016::span::000014","replay_evt_000016::span::000015","replay_evt_000016::span::000016","replay_evt_000017"],"covered_end_sequence":17}
{"generation":3,"content":"# FreetoShop Clone Stamp Milestone — Durable Memory\n\n## Task State\n- **Goal:** Implement FreetoShop, a local-first browser image editor, milestone by milestone.\n- **Current milestone:** Milestone 1 — Clone Stamp foundation.\n- **Phase:** Initial architecture inspection before implementation.\n- **Working directory:** `C:/Users/disti/benchmark_work/freetoshop_pi_vs_orchid_20260820/arm-b-orchid`\n\n## Repository Structure (established)\n- Monorepo with npm workspaces: `apps/web`, `packages/core-document`, `packages/command-history`, `packages/compositor`, `packages/renderer`, `packages/raster-engine`, `packages/selection-engine`, `packages/filter-engine`, `packages/transform-engine`, `packages/text-engine`, `packages/file-codecs`, `packages/persistence`, `packages/benchmarks`, `packages/pixel-storage`.\n- `apps/web/src/` contains: `App.tsx`, `commands/`, `components/`, `editor/`, `panels/`, `shell/`, `tools/`, `ui/`, `workers/`, `workspace/`.\n- `tests/e2e/` exists for browser-level tests.\n- Key docs: `ARCHITECTURE.md`, `DOCUMENT_MODEL.md`, `IMPLEMENTATION_ROADMAP.md`, `TILED_RENDERING.md`, `UI_WORKSPACE_ARCHITECTURE.md`, `WORKER_OPERATIONS.md`, `TESTING.md`.\n\n## Architecture Facts (from ARCHITECTURE.md, DOCUMENT_MODEL.md, raster-engine/index.ts, toolRegistry.ts)\n- **apps/web:** owns interaction, accessibility, PWA, view state. Delegates to `editor/filterOperations`, `editor/types`, `editor/model`, `components/`. `workers/` is the browser-process boundary for long-running local ops.\n- **core-document:** owns serializable editor state, layer ordering, stable IDs, invertible affine layer transforms, validated edits, editable shape geometry/styles, vector paths, text content/styles, ordered typed layer effects, smart-object source trees, smart-filter state, nondestructive canvas geometry. Pixel layers own immutable sparse `TiledPixelStore`; raster masks use `TiledCoverageStore`; copy-on-write patches.\n- **command-history:** applies undoable commands, owns undo/redo history. Command contract: `Command<State>` with `label?` and `execute(state): State`. History stores `before/after` entries, supports undo/redo/clear/reset, capacity default 100.\n- **compositor:** DOM-free premultiplied-alpha source-over, 20 separable + 6 RGB-vector blend modes, immutable/in-place same-sized composition.\n- **renderer:** document + viewport → Canvas 2D pixels; must not mutate document; rasterizes affine pixel/shape/text layers, resolves clipping, group isolation, masks over pass-through/isolated groups, composites smart filters.\n- **raster-engine:** owns deterministic, DOM-free pixel and mask operations. Brush, eraser, and adjustments consume document-space selection coverage and inverse-map affine coordinates, and return minimal layer-local RGBA or one-channel coverage patches. Same mask stroke path edits pixel, shape, text, and smart-object masks through nested world transforms and group masks directly in document space. Image Size uses nearest-neighbor and bilinear pixel-center resamplers for pixel layers, layer masks, selections, and channels; bilinear RGBA interpolation is performed in premultiplied alpha.\n  - Exports: `BrushPoint`, `BrushOptions` (mode/source-over|erase, size, hardness, opacity, color), `MaskBrushOptions` (mode/reveal|conceal, size, hardness, opacity), `CoveragePatch`, `PixelPatch`, `ResamplingMethod` (nearest|bilinear), `ResizeImageOptions`.\n  - Has `sourceCoordinate(destination, sourceSize, destinationSize)` and `clampIndex` helpers; `resampleCoverage`, `resampleCoverageStore`, `resamplePixels`, `resamplePixelStore` for tiled resampling.\n  - **Key implementation patterns observed in raster-engine source:**\n    - `paintStroke(layer, points, options, selection?, worldTransform?)` returns `PixelPatch`; validates layer/transform/selection; builds interpolated dabs from points; computes union of local dab bounds only (no full-layer densify); reads region from `layer.pixelStore.read(...)`; applies per-pixel falloff × opacity × pressure × selection coverage; erase multiplies alpha by (1-coverage), source-over uses `compositeSourceOver`; tracks `changedBounds` and returns `makePatchFromRegion(...)`.\n    - `paintMaskStroke(mask, layerTransform, points, options, selection?)` returns `CoveragePatch`; similar dab/interpolation/bounds pattern for one-channel coverage; reveal target 255, conceal target 0; blends toward target by strength.\n    - `adjustBrightness(layer, amount, selection?, worldTransform?)` returns `PixelPatch`; full-layer path when selection null (entire layer work bounds), sparse path when selection present (densifies only inverse-mapped selection bounds via `layerBoundsFromDocumentBounds`); blends RGB by coverage.\n    - `adjustBrightnessPixelStore(layer, amount, selection?, worldTransform?)` writes directly into immutable canonical tile store one destination tile at a time, avoiding rectangular `PixelPatch` allocation.\n    - `selectionCoverage(selection, documentX, documentY, regionBuffer?, region?)` returns 1 for null selection; samples floor coords; supports region-buffered reads for batch queries; falls back to single-pixel sparse `coverageStore.read({x,y,1,1})`.\n    - `readSelectionRegion(selection, dabs, radius)` computes union of `documentDabBounds` across dabs, reads that rectangular region from coverage store once.\n    - `localDabBounds(transform, inverse, documentX, documentY, radius, width, height)` projects document-space circle into local-space ellipse using inverse linear row lengths `Math.hypot(inverse.a, inverse.c)` and `Math.hypot(inverse.b, inverse.d)`; returns clamped axis-aligned bounds.\n    - `layerBoundsFromDocumentBounds(documentBounds, inverse, layerWidth, layerHeight)` transforms 4 corners through inverse, expands by 1px for soft-edge aliasing, returns null if outside layer.\n    - `compositeSourceOver(pixels, offset, color, coverage)` does premultiplied source-over with alpha recompute.\n    - `dabCoverage(distance, radius, hardness)` soft falloff.\n    - `multiplyTransforms`, `scalePathCommand`, `resizeLayer`, `resizeImage`, `estimateLayerResizeBytes`, `validateOutputSize`, `validateLayer`, `validateSelection`, `validateRasterMask`, `validateTransform`, `transformPoint`, `invertTransform`, `unionBounds`, `documentDabBounds`, `makePatchFromRegion`, `makeCoveragePatchFromRegion`, `include`, `emptyPatch`, `emptyCoveragePatch` all present.\n- **selection-engine:** creates and refines owned, canvas-sized 8-bit coverage masks. Owns supersampled ellipse/polygon rasterization, RMS-color Magic Wand matching, deterministic mask morphology; does not depend on DOM or renderer.\n- **filter-engine:** returns bounded immutable pixel patches for destructive color and Gaussian operations. Maps document-space selection coverage through supplied layer world transform and leaves command/history policy to the caller. Renderer reuses its Gaussian implementation for nondestructive smart-object intermediate surfaces.\n- **transform-engine:** owns DOM-free transform snapshots, local/document handle geometry, nested-parent-aware scaling, rotation, translation, singularity protection. Web app owns pointer capture, previews, accessible controls. Web app projects editable path anchors and cubic controls through nested world transforms; core-document remains canonical geometry/matrix authority.\n- **text-engine:** lazily loads HarfBuzz WebAssembly, owns bounded local-font registration, OpenType substitution/positioning, direction inference, line layout, positioned glyph outlines. Renderer consumes outlines; system fonts without available binaries use explicit Canvas fallback.\n- **file-codecs:** translates PNG/JPEG bytes to/from document model; bounded import-only PSD translation through `ag-psd`. PSD parsing validates binary header before decode and returns explicit compatibility report; does not leak `ag-psd` objects into canonical state. Imported data is untrusted.\n- **persistence:** saves/restores supported state in IndexedDB. No server required; document contents should not leave device implicitly. Owns versioned `.freetoshop` binary container, schema migration, SHA-256 integrity verification, canonical native-file decoding.\n- **benchmarks:** builds versioned synthetic documents and enforces broad CI-safe ceilings for compositor time, brush latency, typed-array storage amplification. Production packages must never depend on it.\n- **pixel-storage:** owns sparse immutable tile stores, copy-on-write pixel patches, sparse one-channel coverage stores, contiguous and tiled `PixelSource` adapters, owned region reads, chunk iteration, validated deterministic worker/persistence snapshots, clipped/coalesced dirty-region accumulation. No dependency on document model or renderer.\n  - Exports: `DEFAULT_TILE_SIZE`, `MAX_TILE_SIZE`, `PixelRegion`, `PixelPatch`, `PixelTile`, `PixelSource`, `PixelWriteResult`, `TiledPixelStoreSnapshot`, `TiledPixelStoreDelta`, `CoveragePatch`, `CoverageSource`, `ContiguousCoverageSource`, `TiledCoverageStore`, `TiledPixelStore`.\n  - `TiledPixelStore` supports `read(region): Uint8ClampedArray`, `write({x,y,width,height,pixels}): PixelWriteResult` (returns new store + dirtyRegion + changedTileKeys), `toContiguous()`, `tileSize`, `width`, `height`.\n  - `TiledCoverageStore` supports similar read/write for one-channel coverage.\n\n## Document Model Facts (from DOCUMENT_MODEL.md)\n- Pixel layers canonically own immutable sparse `TiledPixelStore` resources. Pixel patches use copy-on-write tiles, so history versions may share unchanged tiles without exposing mutable RGBA buffers. Contiguous materialization is an explicit compatibility operation at legacy filter, resize, codec, export, or version-1 persistence boundaries.\n- Raster masks, selections, and saved alpha channels own immutable sparse one-channel `TiledCoverageStore` resources only; no dual contiguous ownership field. Coverage stores carry implicit 8-bit default plus sparse deviation tiles, so partial selection and raster-mask complements remain sparse through inversion, crop, resize, worker transport, native persistence, and selection refinement.\n- Affine selection-to-raster-mask baking reads only active selection tiles and writes a bounded sparse destination rather than allocating full layer-sized coverage buffer. Sparse selection combine reconciles different source tile sizes on one bounded output grid without materializing either full canvas.\n- Cold-composite worker keeps isolated registry of immutable tiled resources. Version-1 task envelopes upload each pixel/coverage snapshot once per store and snapshot scope, then use resource references for later metadata-only document revisions. Version-2 delta envelopes derive next immutable store from retained predecessor, transferring only COW tiles whose buffer identity changed and listing removed tile keys without payload. A task carries its live resource set so superseded stores are evicted on both sides. Abort terminates worker and discards namespace; never reuses references or delta bases across cancellation. Filter, resize, and native-export operations share stable document resource slots in one sequential session. Resize, PSD-import, and native-import results use collision-safe request-scoped resource IDs; worker retains owned clones before transferring buffers, client adopts hydrated stores under same IDs for zero-upload follow-up tasks.\n- Document model is canonical, serializable description of an edit. Document has version, canvas dimensions, ordered layers, stable IDs. Renderable layers refer to image resources rather than DOM or canvas objects. Viewport zoom, pan, selection chrome, dialogs, hover state are transient UI state. Full-resolution selection mask is document state because raster commands, masks, and adjustments consume its 8-bit coverage.\n- Free Transform snapshots, active handles, pointer positions, live previews are transient; only final validated affine matrix enters document history and persistence. Crop drafts are likewise transient; applied crop is represented by new canvas dimensions, translated root transforms, and remapped canvas-sized coverage buffers.\n- Invariants: dimensions, coordinates, opacity, resource sizes are finite and bounded; layer IDs unique within document, layer order deterministic; unsupported layer kinds or enum values fail validation rather than being executed or silently reinterpreted; edits expressed as commands or equivalent pure state transitions so they can be tested and later support undo/redo; saved documents contain data, never executable JavaScript; every layer owns invertible Canvas-style affine matrix (a through f); nested world transforms compose parent × child, supporting translation, rotation, scale, reflection, shear without rewriting source pixels; selection is either null (all pixels) or owned canvas-sized sparse `TiledCoverageStore`; rectangles write only dirty patches; arbitrary masks can replace, add, subtract, or intersect coverage; add uses coverage union; subtract and intersect multiply soft coverage rather than thresholding it; saved alpha channels use same sparse ownership contract; ellipse and lasso results retain 8-bit antialiased edges; Magic Wand produces binary coverage from composite RGBA byte distance; feather, expand, contract, smooth, border produce new owned masks and enter history as one selection command; derived outline geometry and live gesture previews remain view state; selection-engine canvases limited to 32,768 pixels per axis and 100,000,000 pixels total; UI limits refinement radii to 100 pixels; feather is zero-padded square box filter, expand/contract are square maximum/minimum morphology, smooth is square median, border is expanded coverage minus contracted coverage; these are deterministic foundations, not professional Select and Mask/refine-edge model; pixel, shape, text, smart-object, group, and adjustment raster masks own immutable sparse 8-bit coverage stores; their enabled state and normalized density are nondestructive layer properties; painting a structured layer mask never rasterizes its source; mask painting applies immutable bounded coverage patches and never rewrites owning layer's RGBA buffer; pixel/mask targeting is transient UI state; every layer owns validated blend mode, independent opacity/fill opacity, clipping flag, transparency/pixel/position/all lock set; pixel locks reject raster and mask mutation; transparency locks preserve existing alpha during pixel patches; position locks reject affine edits; all locks reject property edits; groups additionally choose pass-through or isolated compositing; adjustment layers own typed Brightness/Contrast, Levels, Hue/Saturation, or Invert payload; optional raster mask is canvas-sized document-space coverage; parameters and masks remain editable and never rewrite underlying pixel layers; shape layers own validated rectangle, ellipse, or arbitrary path geometry and editable fill, stroke, stroke width, and rectangle corner radius; paths own ordered move, line, cubic Bézier, and close commands with finite anchors and controls, bounded to 10,000 commands; text layers own multiline content, fixed layout box, validated family, size, weight, italic, line-height, alignment, color style data; both remain structured rather than storing cached pixels and may carry layer-local raster mask; renderable layers own ordered list of validated color-overlay, outside-stroke, and drop-shadow effects; effect enabled state, opacity, color, blend mode, size, offsets, blur are canonical editable data; source pixels and structured payloads are never baked when an effect changes.\n\n## Tooling/Interaction Model (from toolRegistry.ts, editor/types.ts)\n- Tool registry exists at `apps/web/src/tools/toolRegistry.ts` and `toolRegistry.test.ts`.\n- Existing tools include Brush and Eraser workflows that must be preserved.\n- Toolbar/tool-option architecture exists; Clone Stamp should fit the retouch/painting toolbar family.\n- `Tool` type currently: `'pan' | 'move' | 'transform' | 'crop' | 'select' | 'ellipse' | 'lasso' | 'wand' | 'pen' | 'brush' | 'eraser'`.\n- `EditTarget` type: `'pixels' | 'mask'`.\n- `Gesture` union includes paint gesture: `{ kind: 'paint'; points: BrushPoint[]; layerId: string; target: EditTarget }`.\n- `BrushPoint`: document-space x, y, optional normalized tablet pressure (defaults to 1).\n- `SmartObjectEditSession`: `{ parentHistory, layerId, sourceId, sourceName, parentViewport }`.\n- `DocumentPoint`, `DocumentRectangle`, `ViewportTransform`, `LayerTransform`, `FreeTransformSnapshot`, `TransformHandleId`, `ShapeGeometry`, `PathHandleRole` types available.\n\n## Implementation Constraints (from user goal)\n- Clone Stamp must support: Alt/Option-click to set source; visible source state; aligned mode (preserves source-dest offset across gesture/segments); unaligned mode (restarts from source origin); correct transformed-layer coordinate mapping; respects active selection (clips writes); respects mask/edit-target model; one atomic undoable transaction per gesture; persists/survives save-reopen/recovery; missing source handled visibly and safely.\n- Must use explicit testable data model for clone source/aligned-unaligned/coordinate relationship — not implicit UI-handler state.\n- Reuse tiled stores and coordinate helpers; do not materialize entire canvas/layer for a clone op.\n- Add focused unit tests: source/dest pixel correctness, multi-segment aligned strokes, unaligned restart, transformed layers, selection clipping, masks/edit targets, tile-boundary behavior. Add browser workflow verifying undo/redo + persistence/recovery.\n- Do not regress existing Brush/Eraser behavior.\n\n## Cross-Cutting Acceptance Questions (must answer per feature)\n1. Active edit target (document/layer/mask/channel)?\n2. Coordinate space for pointer/source/selection/layer/document?\n3. Which tiles/bounded region are read, allocated, rendered, written?\n4. Behavior when selection empty, source missing, layer transformed, resource not hydrated, optional backend missing?\n5. Exact command/history boundary for one user action; undo/redo without changing unrelated state?\n6. What is persisted, how reopened, how recovered after interruption?\n\n## Progress to Date (from recent raw source items)\n- **types.ts extended:** Added `Tool = ... | 'cloneStamp'`, `CloneSourceAlignMode = 'aligned' | 'unaligned'`, `CloneSource` interface (x, y, aligned), `CloneStampGesture` interface (kind, points, layerId, target, source, sourceLayerId, destinationWidth/Height, sourceWidth/Height). Added `Gesture` union member `{ kind: 'cloneStamp'; gesture: CloneStampGesture }`.\n- **toolRegistry.ts extended:** Added `'cloneStamp'` to `ToolFamilyId` union.\n- **Still pending:** raster-engine clone painting function, tool registry entry object, ToolOptionsBar UI, command/history integration, unit tests, persistence/recovery story.\n\n## Notes / Next Steps\n- Continue reading ARCHITECTURE.md, DOCUMENT_MODEL.md, toolRegistry.ts, and related editor/model files to identify the correct extension points for Clone Stamp (command/history, tiled stores, coordinate helpers, brush/raster workflow, selection/mask handling).\n- Identify existing brush/raster workflow and command definitions to reuse for clone sampling + destination writes.\n- Plan explicit clone-source data model and aligned/unaligned semantics before writing code.\n- Keep repository buildable; prefer small complete slice over decorative breadth.\n- **Immediate next reads:** `editor/types.ts` full content (already partially read), existing command definitions in `apps/web/src/commands/`, brush/eraser tool implementations, `core-document` layer/patch APIs, `pixel-storage` tile read/write APIs, `command-history` command contract.\n- **Recent reads (raster-engine/src/index.ts spans 5-16):** Confirmed detailed brush/mask/adjustment implementation patterns: `paintStroke`, `paintMaskStroke`, `adjustBrightness`, `adjustBrightnessPixelStore`, `selectionCoverage`, `readSelectionRegion`, `localDabBounds`, `layerBoundsFromDocumentBounds`, `compositeSourceOver`, `dabCoverage`, `multiplyTransforms`, `scalePathCommand`, `resizeLayer`, `resizeImage`, `estimateLayerResizeBytes`, `validateOutputSize`, `validateLayer`, `validateSelection`, `validateRasterMask`, `validateTransform`, `transformPoint`, `invertTransform`, `unionBounds`, `documentDabBounds`, `makePatchFromRegion`, `makeCoveragePatchFromRegion`, `include`, `emptyPatch`, `emptyCoveragePatch`. These are the reusable primitives for clone stamp sampling + destination writes.\n- **Next planned reads:** `packages/core-document/src/index.ts` (layer/patch APIs, document model), `packages/command-history/src/index.ts` (command contract), `apps/web/src/commands/` (existing command definitions), brush/eraser tool implementations, `pixel-storage` tile read/write APIs.","content_hash":"25ccedbf6bf5372de932b2a6265852856ad2a545ebaec401148b458adbfe2cab","base_capsule_hash":"db580def0caf8f926b50e5e8e5f69c72c4400b9cb0acba89385061351001eb2f","source_refs":["replay_evt_000018","replay_evt_000019","replay_evt_000020","replay_evt_000021","replay_evt_000022","replay_evt_000023","replay_evt_000024","replay_evt_000025","replay_evt_000026::span::000000","replay_evt_000026::span::000001","replay_evt_000027","replay_evt_000028","replay_evt_000029","replay_evt_000030","replay_evt_000031","replay_evt_000032","replay_evt_000033","replay_evt_000034","replay_evt_000035","replay_evt_000036","replay_evt_000037","replay_evt_000038","replay_evt_000039","replay_evt_000040"],"covered_end_sequence":40}
{"generation":4,"content":"# FreetoShop Clone Stamp Milestone — Durable Memory\n\n## Task State\n- **Goal:** Implement FreetoShop, a local-first browser image editor, milestone by milestone.\n- **Current milestone:** Milestone 1 — Clone Stamp foundation.\n- **Phase:** Initial architecture inspection before implementation.\n- **Working directory:** `C:/Users/disti/benchmark_work/freetoshop_pi_vs_orchid_20260820/arm-b-orchid`\n\n## Repository Structure (established)\n- Monorepo with npm workspaces: `apps/web`, `packages/core-document`, `packages/command-history`, `packages/compositor`, `packages/renderer`, `packages/raster-engine`, `packages/selection-engine`, `packages/filter-engine`, `packages/transform-engine`, `packages/text-engine`, `packages/file-codecs`, `packages/persistence`, `packages/benchmarks`, `packages/pixel-storage`.\n- `apps/web/src/` contains: `App.tsx`, `commands/`, `components/`, `editor/`, `panels/`, `shell/`, `tools/`, `ui/`, `workers/`, `workspace/`.\n- `tests/e2e/` exists for browser-level tests.\n- Key docs: `ARCHITECTURE.md`, `DOCUMENT_MODEL.md`, `IMPLEMENTATION_ROADMAP.md`, `TILED_RENDERING.md`, `UI_WORKSPACE_ARCHITECTURE.md`, `WORKER_OPERATIONS.md`, `TESTING.md`.\n\n## Architecture Facts (from ARCHITECTURE.md, DOCUMENT_MODEL.md, raster-engine/index.ts, toolRegistry.ts)\n- **apps/web:** owns interaction, accessibility, PWA, view state. Delegates to `editor/filterOperations`, `editor/types`, `editor/model`, `components/`. `workers/` is the browser-process boundary for long-running local ops.\n- **core-document:** owns serializable editor state, layer ordering, stable IDs, invertible affine layer transforms, validated edits, editable shape geometry/styles, vector paths, text content/styles, ordered typed layer effects, smart-object source trees, smart-filter state, nondestructive canvas geometry. Pixel layers own immutable sparse `TiledPixelStore`; raster masks use `TiledCoverageStore`; copy-on-write patches.\n- **command-history:** applies undoable commands, owns undo/redo history. Command contract: `Command<State>` with `label?` and `execute(state): State`. History stores `before/after` entries, supports undo/redo/clear/reset, capacity default 100.\n- **compositor:** DOM-free premultiplied-alpha source-over, 20 separable + 6 RGB-vector blend modes, immutable/in-place same-sized composition.\n- **renderer:** document + viewport → Canvas 2D pixels; must not mutate document; rasterizes affine pixel/shape/text layers, resolves clipping, group isolation, masks over pass-through/isolated groups, composites smart filters.\n- **raster-engine:** owns deterministic, DOM-free pixel and mask operations. Brush, eraser, and adjustments consume document-space selection coverage and inverse-map affine coordinates, and return minimal layer-local RGBA or one-channel coverage patches. Same mask stroke path edits pixel, shape, text, and smart-object masks through nested world transforms and group masks directly in document space. Image Size uses nearest-neighbor and bilinear pixel-center resamplers for pixel layers, layer masks, selections, and channels; bilinear RGBA interpolation is performed in premultiplied alpha.\n  - Exports: `BrushPoint`, `BrushOptions` (mode/source-over|erase, size, hardness, opacity, color), `MaskBrushOptions` (mode/reveal|conceal, size, hardness, opacity), `CoveragePatch`, `PixelPatch`, `ResamplingMethod` (nearest|bilinear), `ResizeImageOptions`.\n  - Has `sourceCoordinate(destination, sourceSize, destinationSize)` and `clampIndex` helpers; `resampleCoverage`, `resampleCoverageStore`, `resamplePixels`, `resamplePixelStore` for tiled resampling.\n  - **Key implementation patterns observed in raster-engine source:**\n    - `paintStroke(layer, points, options, selection?, worldTransform?)` returns `PixelPatch`; validates layer/transform/selection; builds interpolated dabs from points; computes union of local dab bounds only (no full-layer densify); reads region from `layer.pixelStore.read(...)`; applies per-pixel falloff × opacity × pressure × selection coverage; erase multiplies alpha by (1-coverage), source-over uses `compositeSourceOver`; tracks `changedBounds` and returns `makePatchFromRegion(...)`.\n    - `paintMaskStroke(mask, layerTransform, points, options, selection?)` returns `CoveragePatch`; similar dab/interpolation/bounds pattern for one-channel coverage; reveal target 255, conceal target 0; blends toward target by strength.\n    - `adjustBrightness(layer, amount, selection?, worldTransform?)` returns `PixelPatch`; full-layer path when selection null (entire layer work bounds), sparse path when selection present (densifies only inverse-mapped selection bounds via `layerBoundsFromDocumentBounds`); blends RGB by coverage.\n    - `adjustBrightnessPixelStore(layer, amount, selection?, worldTransform?)` writes directly into immutable canonical tile store one destination tile at a time, avoiding rectangular `PixelPatch` allocation.\n    - `selectionCoverage(selection, documentX, documentY, regionBuffer?, region?)` returns 1 for null selection; samples floor coords; supports region-buffered reads for batch queries; falls back to single-pixel sparse `coverageStore.read({x,y,1,1})`.\n    - `readSelectionRegion(selection, dabs, radius)` computes union of `documentDabBounds` across dabs, reads that rectangular region from coverage store once.\n    - `localDabBounds(transform, inverse, documentX, documentY, radius, width, height)` projects document-space circle into local-space ellipse using inverse linear row lengths `Math.hypot(inverse.a, inverse.c)` and `Math.hypot(inverse.b, inverse.d)`; returns clamped axis-aligned bounds.\n    - `layerBoundsFromDocumentBounds(documentBounds, inverse, layerWidth, layerHeight)` transforms 4 corners through inverse, expands by 1px for soft-edge aliasing, returns null if outside layer.\n    - `compositeSourceOver(pixels, offset, color, coverage)` does premultiplied source-over with alpha recompute.\n    - `dabCoverage(distance, radius, hardness)` soft falloff.\n    - `multiplyTransforms`, `scalePathCommand`, `resizeLayer`, `resizeImage`, `estimateLayerResizeBytes`, `validateOutputSize`, `validateLayer`, `validateSelection`, `validateRasterMask`, `validateTransform`, `transformPoint`, `invertTransform`, `unionBounds`, `documentDabBounds`, `makePatchFromRegion`, `makeCoveragePatchFromRegion`, `include`, `emptyPatch`, `emptyCoveragePatch` all present.\n- **selection-engine:** creates and refines owned, canvas-sized 8-bit coverage masks. Owns supersampled ellipse/polygon rasterization, RMS-color Magic Wand matching, deterministic mask morphology; does not depend on DOM or renderer.\n- **filter-engine:** returns bounded immutable pixel patches for destructive color and Gaussian operations. Maps document-space selection coverage through supplied layer world transform and leaves command/history policy to the caller. Renderer reuses its Gaussian implementation for nondestructive smart-object intermediate surfaces.\n- **transform-engine:** owns DOM-free transform snapshots, local/document handle geometry, nested-parent-aware scaling, rotation, translation, singularity protection. Web app owns pointer capture, previews, accessible controls. Web app projects editable path anchors and cubic controls through nested world transforms; core-document remains canonical geometry/matrix authority.\n- **text-engine:** lazily loads HarfBuzz WebAssembly, owns bounded local-font registration, OpenType substitution/positioning, direction inference, line layout, positioned glyph outlines. Renderer consumes outlines; system fonts without available binaries use explicit Canvas fallback.\n- **file-codecs:** translates PNG/JPEG bytes to/from document model; bounded import-only PSD translation through `ag-psd`. PSD parsing validates binary header before decode and returns explicit compatibility report; does not leak `ag-psd` objects into canonical state. Imported data is untrusted.\n- **persistence:** saves/restores supported state in IndexedDB. No server required; document contents should not leave device implicitly. Owns versioned `.freetoshop` binary container, schema migration, SHA-256 integrity verification, canonical native-file decoding.\n- **benchmarks:** builds versioned synthetic documents and enforces broad CI-safe ceilings for compositor time, brush latency, typed-array storage amplification. Production packages must never depend on it.\n- **pixel-storage:** owns sparse immutable tile stores, copy-on-write pixel patches, sparse one-channel coverage stores, contiguous and tiled `PixelSource` adapters, owned region reads, chunk iteration, validated deterministic worker/persistence snapshots, clipped/coalesced dirty-region accumulation. No dependency on document model or renderer.\n  - Exports: `DEFAULT_TILE_SIZE`, `MAX_TILE_SIZE`, `PixelRegion`, `PixelPatch`, `PixelTile`, `PixelSource`, `PixelWriteResult`, `TiledPixelStoreSnapshot`, `TiledPixelStoreDelta`, `CoveragePatch`, `CoverageSource`, `ContiguousCoverageSource`, `TiledCoverageStore`, `TiledPixelStore`.\n  - `TiledPixelStore` supports `read(region): Uint8ClampedArray`, `write({x,y,width,height,pixels}): PixelWriteResult` (returns new store + dirtyRegion + changedTileKeys), `toContiguous()`, `tileSize`, `width`, `height`.\n  - `TiledCoverageStore` supports similar read/write for one-channel coverage.\n\n## Document Model Facts (from DOCUMENT_MODEL.md)\n- Pixel layers canonically own immutable sparse `TiledPixelStore` resources. Pixel patches use copy-on-write tiles, so history versions may share unchanged tiles without exposing mutable RGBA buffers. Contiguous materialization is an explicit compatibility operation at legacy filter, resize, codec, export, or version-1 persistence boundaries.\n- Raster masks, selections, and saved alpha channels own immutable sparse one-channel `TiledCoverageStore` resources only; no dual contiguous ownership field. Coverage stores carry implicit 8-bit default plus sparse deviation tiles, so partial selection and raster-mask complements remain sparse through inversion, crop, resize, worker transport, native persistence, and selection refinement.\n- Affine selection-to-raster-mask baking reads only active selection tiles and writes a bounded sparse destination rather than allocating full layer-sized coverage buffer. Sparse selection combine reconciles different source tile sizes on one bounded output grid without materializing either full canvas.\n- Cold-composite worker keeps isolated registry of immutable tiled resources. Version-1 task envelopes upload each pixel/coverage snapshot once per store and snapshot scope, then use resource references for later metadata-only document revisions. Version-2 delta envelopes derive next immutable store from retained predecessor, transferring only COW tiles whose buffer identity changed and listing removed tile keys without payload. A task carries its live resource set so superseded stores are evicted on both sides. Abort terminates worker and discards namespace; never reuses references or delta bases across cancellation. Filter, resize, and native-export operations share stable document resource slots in one sequential session. Resize, PSD-import, and native-import results use collision-safe request-scoped resource IDs; worker retains owned clones before transferring buffers, client adopts hydrated stores under same IDs for zero-upload follow-up tasks.\n- Document model is canonical, serializable description of an edit. Document has version, canvas dimensions, ordered layers, stable IDs. Renderable layers refer to image resources rather than DOM or canvas objects. Viewport zoom, pan, selection chrome, dialogs, hover state are transient UI state. Full-resolution selection mask is document state because raster commands, masks, and adjustments consume its 8-bit coverage.\n- Free Transform snapshots, active handles, pointer positions, live previews are transient; only final validated affine matrix enters document history and persistence. Crop drafts are likewise transient; applied crop is represented by new canvas dimensions, translated root transforms, and remapped canvas-sized coverage buffers.\n- Invariants: dimensions, coordinates, opacity, resource sizes are finite and bounded; layer IDs unique within document, layer order deterministic; unsupported layer kinds or enum values fail validation rather than being executed or silently reinterpreted; edits expressed as commands or equivalent pure state transitions so they can be tested and later support undo/redo; saved documents contain data, never executable JavaScript; every layer owns invertible Canvas-style affine matrix (a through f); nested world transforms compose parent × child, supporting translation, rotation, scale, reflection, shear without rewriting source pixels; selection is either null (all pixels) or owned canvas-sized sparse `TiledCoverageStore`; rectangles write only dirty patches; arbitrary masks can replace, add, subtract, or intersect coverage; add uses coverage union; subtract and intersect multiply soft coverage rather than thresholding it; saved alpha channels use same sparse ownership contract; ellipse and lasso results retain 8-bit antialiased edges; Magic Wand produces binary coverage from composite RGBA byte distance; feather, expand, contract, smooth, border produce new owned masks and enter history as one selection command; derived outline geometry and live gesture previews remain view state; selection-engine canvases limited to 32,768 pixels per axis and 100,000,000 pixels total; UI limits refinement radii to 100 pixels; feather is zero-padded square box filter, expand/contract are square maximum/minimum morphology, smooth is square median, border is expanded coverage minus contracted coverage; these are deterministic foundations, not professional Select and Mask/refine-edge model; pixel, shape, text, smart-object, group, and adjustment raster masks own immutable sparse 8-bit coverage stores; their enabled state and normalized density are nondestructive layer properties; painting a structured layer mask never rasterizes its source; mask painting applies immutable bounded coverage patches and never rewrites owning layer's RGBA buffer; pixel/mask targeting is transient UI state; every layer owns validated blend mode, independent opacity/fill opacity, clipping flag, transparency/pixel/position/all lock set; pixel locks reject raster and mask mutation; transparency locks preserve existing alpha during pixel patches; position locks reject affine edits; all locks reject property edits; groups additionally choose pass-through or isolated compositing; adjustment layers own typed Brightness/Contrast, Levels, Hue/Saturation, or Invert payload; optional raster mask is canvas-sized document-space coverage; parameters and masks remain editable and never rewrite underlying pixel layers; shape layers own validated rectangle, ellipse, or arbitrary path geometry and editable fill, stroke, stroke width, and rectangle corner radius; paths own ordered move, line, cubic Bézier, and close commands with finite anchors and controls, bounded to 10,000 commands; text layers own multiline content, fixed layout box, validated family, size, weight, italic, line-height, alignment, color style data; both remain structured rather than storing cached pixels and may carry layer-local raster mask; renderable layers own ordered list of validated color-overlay, outside-stroke, and drop-shadow effects; effect enabled state, opacity, color, blend mode, size, offsets, blur are canonical editable data; source pixels and structured payloads are never baked when an effect changes.\n\n## Tooling/Interaction Model (from toolRegistry.ts, editor/types.ts)\n- Tool registry exists at `apps/web/src/tools/toolRegistry.ts` and `toolRegistry.test.ts`.\n- Existing tools include Brush and Eraser workflows that must be preserved.\n- Toolbar/tool-option architecture exists; Clone Stamp should fit the retouch/painting toolbar family.\n- `Tool` type currently: `'pan' | 'move' | 'transform' | 'crop' | 'select' | 'ellipse' | 'lasso' | 'wand' | 'pen' | 'brush' | 'eraser'`.\n- `EditTarget` type: `'pixels' | 'mask'`.\n- `Gesture` union includes paint gesture: `{ kind: 'paint'; points: BrushPoint[]; layerId: string; target: EditTarget }`.\n- `BrushPoint`: document-space x, y, optional normalized tablet pressure (defaults to 1).\n- `SmartObjectEditSession`: `{ parentHistory, layerId, sourceId, sourceName, parentViewport }`.\n- `DocumentPoint`, `DocumentRectangle`, `ViewportTransform`, `LayerTransform`, `FreeTransformSnapshot`, `TransformHandleId`, `ShapeGeometry`, `PathHandleRole` types available.\n\n## Implementation Constraints (from user goal)\n- Clone Stamp must support: Alt/Option-click to set source; visible source state; aligned mode (preserves source-dest offset across gesture/segments); unaligned mode (restarts from source origin); correct transformed-layer coordinate mapping; respects active selection (clips writes); respects mask/edit-target model; one atomic undoable transaction per gesture; persists/survives save-reopen/recovery; missing source handled visibly and safely.\n- Must use explicit testable data model for clone source/aligned-unaligned/coordinate relationship — not implicit UI-handler state.\n- Reuse tiled stores and coordinate helpers; do not materialize entire canvas/layer for a clone op.\n- Add focused unit tests: source/dest pixel correctness, multi-segment aligned strokes, unaligned restart, transformed layers, selection clipping, masks/edit targets, tile-boundary behavior. Add browser workflow verifying undo/redo + persistence/recovery.\n- Do not regress existing Brush/Eraser behavior.\n\n## Cross-Cutting Acceptance Questions (must answer per feature)\n1. Active edit target (document/layer/mask/channel)?\n2. Coordinate space for pointer/source/selection/layer/document?\n3. Which tiles/bounded region are read, allocated, rendered, written?\n4. Behavior when selection empty, source missing, layer transformed, resource not hydrated, optional backend missing?\n5. Exact command/history boundary for one user action; undo/redo without changing unrelated state?\n6. What is persisted, how reopened, how recovered after interruption?\n\n## Progress to Date (from recent raw source items)\n- **types.ts extended:** Added `Tool = ... | 'cloneStamp'`, `CloneSourceAlignMode = 'aligned' | 'unaligned'`, `CloneSource` interface (x, y, aligned), `CloneStampGesture` interface (kind, points, layerId, target, source, sourceLayerId, destinationWidth/Height, sourceWidth/Height). Added `Gesture` union member `{ kind: 'cloneStamp'; gesture: CloneStampGesture }`.\n- **toolRegistry.ts extended:** Added `'cloneStamp'` to `ToolFamilyId` union; added `ToolDefinition` entry for `cloneStamp` tool with label 'Clone Stamp Tool', accessibleLabel 'Clone Stamp', icon 'clone', needsDocument true, family 'cloneStamp', slot 12, shortcut 'S'.\n- **Icon.tsx extended:** Added 'clone' to `ICON_NAMES` and added clone icon path definition.\n- **raster-engine/src/index.ts extended:** Added `CloneStampOptions` interface (size, hardness, opacity, aligned, sourceX, sourceY) and `paintCloneStamp` function skeleton. The current implementation is a placeholder that returns `emptyPatch()` and needs to be properly implemented with the correct clone stamp logic.\n- **Still pending:** Complete `paintCloneStamp` implementation with proper aligned/unaligned source sampling, tool registry entry object, ToolOptionsBar UI, command/history integration, unit tests, persistence/recovery story.\n\n## Notes / Next Steps\n- Continue reading ARCHITECTURE.md, DOCUMENT_MODEL.md, toolRegistry.ts, and related editor/model files to identify the correct extension points for Clone Stamp (command/history, tiled stores, coordinate helpers, brush/raster workflow, selection/mask handling).\n- Identify existing brush/raster workflow and command definitions to reuse for clone sampling + destination writes.\n- Plan explicit clone-source data model and aligned/unaligned semantics before writing code.\n- Keep repository buildable; prefer small complete slice over decorative breadth.\n- **Immediate next reads:** `editor/types.ts` full content (already partially read), existing command definitions in `apps/web/src/commands/`, brush/eraser tool implementations, `core-document` layer/patch APIs, `pixel-storage` tile read/write APIs, `command-history` command contract.\n- **Recent reads (raster-engine/src/index.ts spans 5-16):** Confirmed detailed brush/mask/adjustment implementation patterns: `paintStroke`, `paintMaskStroke`, `adjustBrightness`, `adjustBrightnessPixelStore`, `selectionCoverage`, `readSelectionRegion`, `localDabBounds`, `layerBoundsFromDocumentBounds`, `compositeSourceOver`, `dabCoverage`, `multiplyTransforms`, `scalePathCommand`, `resizeLayer`, `resizeImage`, `estimateLayerResizeBytes`, `validateOutputSize`, `validateLayer`, `validateSelection`, `validateRasterMask`, `validateTransform`, `transformPoint`, `invertTransform`, `unionBounds`, `documentDabBounds`, `makePatchFromRegion`, `makeCoveragePatchFromRegion`, `include`, `emptyPatch`, `emptyCoveragePatch`. These are the reusable primitives for clone stamp sampling + destination writes.\n- **Next planned reads:** `packages/core-document/src/index.ts` (layer/patch APIs, document model), `packages/command-history/src/index.ts` (command contract), `apps/web/src/commands/` (existing command definitions), brush/eraser tool implementations, `pixel-storage` tile read/write APIs.\n- **Recent implementation activity:** Added cloneStamp tool to toolRegistry.ts with icon 'clone' (which was added to Icon.tsx). Started implementing `paintCloneStamp` in raster-engine/src/index.ts with `CloneStampOptions` interface including `sourceX`/`sourceY` fields and `aligned` boolean. The function skeleton validates inputs and sets up inverse transforms but the core sampling/writing logic is not yet complete — it currently returns `emptyPatch()` as placeholder. The implementation needs to: convert source position from document space to source layer local space, interpolate points into dabs with source positions calculated per dab (aligned offset from first point vs unaligned constant source), read source pixels, apply falloff/opacity/pressure/selection coverage, write to destination using source-over compositing, track changed bounds, return patch.","content_hash":"9bbfb9c54e8dfdf7b07f7e603cd00cd6e710b3b7424fe210c4e4a5b4b43abcb0","base_capsule_hash":"25ccedbf6bf5372de932b2a6265852856ad2a545ebaec401148b458adbfe2cab","source_refs":["replay_evt_000041","replay_evt_000042::span::000000","replay_evt_000042::span::000001","replay_evt_000043","replay_evt_000044","replay_evt_000045","replay_evt_000046::span::000000","replay_evt_000046::span::000001","replay_evt_000047","replay_evt_000048","replay_evt_000049","replay_evt_000050","replay_evt_000051","replay_evt_000052","replay_evt_000053","replay_evt_000054","replay_evt_000055","replay_evt_000056","replay_evt_000057","replay_evt_000058","replay_evt_000059::span::000000","replay_evt_000059::span::000001","replay_evt_000059::span::000002","replay_evt_000060","replay_evt_000061::span::000000"],"covered_end_sequence":61}
{"generation":6,"content":"# FreetoShop Clone Stamp Milestone — Durable Memory\n\n## Task State\n- **Goal:** Implement FreetoShop, a local-first browser image editor, milestone by milestone.\n- **Current milestone:** Milestone 1 — Clone Stamp foundation.\n- **Phase:** Initial architecture inspection before implementation.\n- **Working directory:** `C:/Users/disti/benchmark_work/freetoshop_pi_vs_orchid_20260820/arm-b-orchid`\n\n## Repository Structure (established)\n- Monorepo with npm workspaces: `apps/web`, `packages/core-document`, `packages/command-history`, `packages/compositor`, `packages/renderer`, `packages/raster-engine`, `packages/selection-engine`, `packages/filter-engine`, `packages/transform-engine`, `packages/text-engine`, `packages/file-codecs`, `packages/persistence`, `packages/benchmarks`, `packages/pixel-storage`.\n- `apps/web/src/` contains: `App.tsx`, `commands/`, `components/`, `editor/`, `panels/`, `shell/`, `tools/`, `ui/`, `workers/`, `workspace/`.\n- `tests/e2e/` exists for browser-level tests.\n- Key docs: `ARCHITECTURE.md`, `DOCUMENT_MODEL.md`, `IMPLEMENTATION_ROADMAP.md`, `TILED_RENDERING.md`, `UI_WORKSPACE_ARCHITECTURE.md`, `WORKER_OPERATIONS.md`, `TESTING.md`.\n\n## Architecture Facts (from ARCHITECTURE.md, DOCUMENT_MODEL.md, raster-engine/index.ts, toolRegistry.ts)\n- **apps/web:** owns interaction, accessibility, PWA, view state. Delegates to `editor/filterOperations`, `editor/types`, `editor/model`, `components/`. `workers/` is the browser-process boundary for long-running local ops.\n- **core-document:** owns serializable editor state, layer ordering, stable IDs, invertible affine layer transforms, validated edits, editable shape geometry/styles, vector paths, text content/styles, ordered typed layer effects, smart-object source trees, smart-filter state, nondestructive canvas geometry. Pixel layers own immutable sparse `TiledPixelStore`; raster masks use `TiledCoverageStore`; copy-on-write patches.\n- **command-history:** applies undoable commands, owns undo/redo history. Command contract: `Command<State>` with `label?` and `execute(state): State`. History stores `before/after` entries, supports undo/redo/clear/reset, capacity default 100.\n- **compositor:** DOM-free premultiplied-alpha source-over, 20 separable + 6 RGB-vector blend modes, immutable/in-place same-sized composition.\n- **renderer:** document + viewport → Canvas 2D pixels; must not mutate document; rasterizes affine pixel/shape/text layers, resolves clipping, group isolation, masks over pass-through/isolated groups, composites smart filters.\n- **raster-engine:** owns deterministic, DOM-free pixel and mask operations. Brush, eraser, and adjustments consume document-space selection coverage and inverse-map affine coordinates, and return minimal layer-local RGBA or one-channel coverage patches. Same mask stroke path edits pixel, shape, text, and smart-object masks through nested world transforms and group masks directly in document space. Image Size uses nearest-neighbor and bilinear pixel-center resamplers for pixel layers, layer masks, selections, and channels; bilinear RGBA interpolation is performed in premultiplied alpha.\n  - Exports: `BrushPoint`, `BrushOptions` (mode/source-over|erase, size, hardness, opacity, color), `MaskBrushOptions` (mode/reveal|conceal, size, hardness, opacity), `CoveragePatch`, `PixelPatch`, `ResamplingMethod` (nearest|bilinear), `ResizeImageOptions`.\n  - Has `sourceCoordinate(destination, sourceSize, destinationSize)` and `clampIndex` helpers; `resampleCoverage`, `resampleCoverageStore`, `resamplePixels`, `resamplePixelStore` for tiled resampling.\n  - **Key implementation patterns observed in raster-engine source:**\n    - `paintStroke(layer, points, options, selection?, worldTransform?)` returns `PixelPatch`; validates layer/transform/selection; builds interpolated dabs from points; computes union of local dab bounds only (no full-layer densify); reads region from `layer.pixelStore.read(...)`; applies per-pixel falloff × opacity × pressure × selection coverage; erase multiplies alpha by (1-coverage), source-over uses `compositeSourceOver`; tracks `changedBounds` and returns `makePatchFromRegion(...)`.\n    - `paintMaskStroke(mask, layerTransform, points, options, selection?)` returns `CoveragePatch`; similar dab/interpolation/bounds pattern for one-channel coverage; reveal target 255, conceal target 0; blends toward target by strength.\n    - `adjustBrightness(layer, amount, selection?, worldTransform?)` returns `PixelPatch`; full-layer path when selection null (entire layer work bounds), sparse path when selection present (densifies only inverse-mapped selection bounds via `layerBoundsFromDocumentBounds`); blends RGB by coverage.\n    - `adjustBrightnessPixelStore(layer, amount, selection?, worldTransform?)` writes directly into immutable canonical tile store one destination tile at a time, avoiding rectangular `PixelPatch` allocation.\n    - `selectionCoverage(selection, documentX, documentY, regionBuffer?, region?)` returns 1 for null selection; samples floor coords; supports region-buffered reads for batch queries; falls back to single-pixel sparse `coverageStore.read({x,y,1,1})`.\n    - `readSelectionRegion(selection, dabs, radius)` computes union of `documentDabBounds` across dabs, reads that rectangular region from coverage store once.\n    - `localDabBounds(transform, inverse, documentX, documentY, radius, width, height)` projects document-space circle into local-space ellipse using inverse linear row lengths `Math.hypot(inverse.a, inverse.c)` and `Math.hypot(inverse.b, inverse.d)`; returns clamped axis-aligned bounds.\n    - `layerBoundsFromDocumentBounds(documentBounds, inverse, layerWidth, layerHeight)` transforms 4 corners through inverse, expands by 1px for soft-edge aliasing, returns null if outside layer.\n    - `compositeSourceOver(pixels, offset, color, coverage)` does premultiplied source-over with alpha recompute.\n    - `dabCoverage(distance, radius, hardness)` soft falloff.\n    - `multiplyTransforms`, `scalePathCommand`, `resizeLayer`, `resizeImage`, `estimateLayerResizeBytes`, `validateOutputSize`, `validateLayer`, `validateSelection`, `validateRasterMask`, `validateTransform`, `transformPoint`, `invertTransform`, `unionBounds`, `documentDabBounds`, `makePatchFromRegion`, `makeCoveragePatchFromRegion`, `include`, `emptyPatch`, `emptyCoveragePatch` all present.\n- **selection-engine:** creates and refines owned, canvas-sized 8-bit coverage masks. Owns supersampled ellipse/polygon rasterization, RMS-color Magic Wand matching, deterministic mask morphology; does not depend on DOM or renderer.\n- **filter-engine:** returns bounded immutable pixel patches for destructive color and Gaussian operations. Maps document-space selection coverage through supplied layer world transform and leaves command/history policy to the caller. Renderer reuses its Gaussian implementation for nondestructive smart-object intermediate surfaces.\n- **transform-engine:** owns DOM-free transform snapshots, local/document handle geometry, nested-parent-aware scaling, rotation, translation, singularity protection. Web app owns pointer capture, previews, accessible controls. Web app projects editable path anchors and cubic controls through nested world transforms; core-document remains canonical geometry/matrix authority.\n- **text-engine:** lazily loads HarfBuzz WebAssembly, owns bounded local-font registration, OpenType substitution/positioning, direction inference, line layout, positioned glyph outlines. Renderer consumes outlines; system fonts without available binaries use explicit Canvas fallback.\n- **file-codecs:** translates PNG/JPEG bytes to/from document model; bounded import-only PSD translation through `ag-psd`. PSD parsing validates binary header before decode and returns explicit compatibility report; does not leak `ag-psd` objects into canonical state. Imported data is untrusted.\n- **persistence:** saves/restores supported state in IndexedDB. No server required; document contents should not leave device implicitly. Owns versioned `.freetoshop` binary container, schema migration, SHA-256 integrity verification, canonical native-file decoding.\n- **benchmarks:** builds versioned synthetic documents and enforces broad CI-safe ceilings for compositor time, brush latency, typed-array storage amplification. Production packages must never depend on it.\n- **pixel-storage:** owns sparse immutable tile stores, copy-on-write pixel patches, sparse one-channel coverage stores, contiguous and tiled `PixelSource` adapters, owned region reads, chunk iteration, validated deterministic worker/persistence snapshots, clipped/coalesced dirty-region accumulation. No dependency on document model or renderer.\n  - Exports: `DEFAULT_TILE_SIZE`, `MAX_TILE_SIZE`, `PixelRegion`, `PixelPatch`, `PixelTile`, `PixelSource`, `PixelWriteResult`, `TiledPixelStoreSnapshot`, `TiledPixelStoreDelta`, `CoveragePatch`, `CoverageSource`, `ContiguousCoverageSource`, `TiledCoverageStore`, `TiledPixelStore`.\n  - `TiledPixelStore` supports `read(region): Uint8ClampedArray`, `write({x,y,width,height,pixels}): PixelWriteResult` (returns new store + dirtyRegion + changedTileKeys), `toContiguous()`, `tileSize`, `width`, `height`.\n  - `TiledCoverageStore` supports similar read/write for one-channel coverage.\n\n## Document Model Facts (from DOCUMENT_MODEL.md)\n- Pixel layers canonically own immutable sparse `TiledPixelStore` resources. Pixel patches use copy-on-write tiles, so history versions may share unchanged tiles without exposing mutable RGBA buffers. Contiguous materialization is an explicit compatibility operation at legacy filter, resize, codec, export, or version-1 persistence boundaries.\n- Raster masks, selections, and saved alpha channels own immutable sparse one-channel `TiledCoverageStore` resources only; no dual contiguous ownership field. Coverage stores carry implicit 8-bit default plus sparse deviation tiles, so partial selection and raster-mask complements remain sparse through inversion, crop, resize, worker transport, native persistence, and selection refinement.\n- Affine selection-to-raster-mask baking reads only active selection tiles and writes a bounded sparse destination rather than allocating full layer-sized coverage buffer. Sparse selection combine reconciles different source tile sizes on one bounded output grid without materializing either full canvas.\n- Cold-composite worker keeps isolated registry of immutable tiled resources. Version-1 task envelopes upload each pixel/coverage snapshot once per store and snapshot scope, then use resource references for later metadata-only document revisions. Version-2 delta envelopes derive next immutable store from retained predecessor, transferring only COW tiles whose buffer identity changed and listing removed tile keys without payload. A task carries its live resource set so superseded stores are evicted on both sides. Abort terminates worker and discards namespace; never reuses references or delta bases across cancellation. Filter, resize, and native-export operations share stable document resource slots in one sequential session. Resize, PSD-import, and native-import results use collision-safe request-scoped resource IDs; worker retains owned clones before transferring buffers, client adopts hydrated stores under same IDs for zero-upload follow-up tasks.\n- Document model is canonical, serializable description of an edit. Document has version, canvas dimensions, ordered layers, stable IDs. Renderable layers refer to image resources rather than DOM or canvas objects. Viewport zoom, pan, selection chrome, dialogs, hover state are transient UI state. Full-resolution selection mask is document state because raster commands, masks, and adjustments consume its 8-bit coverage.\n- Free Transform snapshots, active handles, pointer positions, live previews are transient; only final validated affine matrix enters document history and persistence. Crop drafts are likewise transient; applied crop is represented by new canvas dimensions, translated root transforms, and remapped canvas-sized coverage buffers.\n- Invariants: dimensions, coordinates, opacity, resource sizes are finite and bounded; layer IDs unique within document, layer order deterministic; unsupported layer kinds or enum values fail validation rather than being executed or silently reinterpreted; edits expressed as commands or equivalent pure state transitions so they can be tested and later support undo/redo; saved documents contain data, never executable JavaScript; every layer owns invertible Canvas-style affine matrix (a through f); nested world transforms compose parent × child, supporting translation, rotation, scale, reflection, shear without rewriting source pixels; selection is either null (all pixels) or owned canvas-sized sparse `TiledCoverageStore`; rectangles write only dirty patches; arbitrary masks can replace, add, subtract, or intersect coverage; add uses coverage union; subtract and intersect multiply soft coverage rather than thresholding it; saved alpha channels use same sparse ownership contract; ellipse and lasso results retain 8-bit antialiased edges; Magic Wand produces binary coverage from composite RGBA byte distance; feather, expand, contract, smooth, border produce new owned masks and enter history as one selection command; derived outline geometry and live gesture previews remain view state; selection-engine canvases limited to 32,768 pixels per axis and 100,000,000 pixels total; UI limits refinement radii to 100 pixels; feather is zero-padded square box filter, expand/contract are square maximum/minimum morphology, smooth is square median, border is expanded coverage minus contracted coverage; these are deterministic foundations, not professional Select and Mask/refine-edge model; pixel, shape, text, smart-object, group, and adjustment raster masks own immutable sparse 8-bit coverage stores; their enabled state and normalized density are nondestructive layer properties; painting a structured layer mask never rasterizes its source; mask painting applies immutable bounded coverage patches and never rewrites owning layer's RGBA buffer; pixel/mask targeting is transient UI state; every layer owns validated blend mode, independent opacity/fill opacity, clipping flag, transparency/pixel/position/all lock set; pixel locks reject raster and mask mutation; transparency locks preserve existing alpha during pixel patches; position locks reject affine edits; all locks reject property edits; groups additionally choose pass-through or isolated compositing; adjustment layers own typed Brightness/Contrast, Levels, Hue/Saturation, or Invert payload; optional raster mask is canvas-sized document-space coverage; parameters and masks remain editable and never rewrite underlying pixel layers; shape layers own validated rectangle, ellipse, or arbitrary path geometry and editable fill, stroke, stroke width, and rectangle corner radius; paths own ordered move, line, cubic Bézier, and close commands with finite anchors and controls, bounded to 10,000 commands; text layers own multiline content, fixed layout box, validated family, size, weight, italic, line-height, alignment, color style data; both remain structured rather than storing cached pixels and may carry layer-local raster mask; renderable layers own ordered list of validated color-overlay, outside-stroke, and drop-shadow effects; effect enabled state, opacity, color, blend mode, size, offsets, blur are canonical editable data; source pixels and structured payloads are never baked when an effect changes.\n\n## Tooling/Interaction Model (from toolRegistry.ts, editor/types.ts)\n- Tool registry exists at `apps/web/src/tools/toolRegistry.ts` and `toolRegistry.test.ts`.\n- Existing tools include Brush and Eraser workflows that must be preserved.\n- Toolbar/tool-option architecture exists; Clone Stamp should fit the retouch/painting toolbar family.\n- `Tool` type currently: `'pan' | 'move' | 'transform' | 'crop' | 'select' | 'ellipse' | 'lasso' | 'wand' | 'pen' | 'brush' | 'eraser'`.\n- `EditTarget` type: `'pixels' | 'mask'`.\n- `Gesture` union includes paint gesture: `{ kind: 'paint'; points: BrushPoint[]; layerId: string; target: EditTarget }`.\n- `BrushPoint`: document-space x, y, optional normalized tablet pressure (defaults to 1).\n- `SmartObjectEditSession`: `{ parentHistory, layerId, sourceId, sourceName, parentViewport }`.\n- `DocumentPoint`, `DocumentRectangle`, `ViewportTransform`, `LayerTransform`, `FreeTransformSnapshot`, `TransformHandleId`, `ShapeGeometry`, `PathHandleRole` types available.\n\n## Implementation Constraints (from user goal)\n- Clone Stamp must support: Alt/Option-click to set source; visible source state; aligned mode (preserves source-dest offset across gesture/segments); unaligned mode (restarts from source origin); correct transformed-layer coordinate mapping; respects active selection (clips writes); respects mask/edit-target model; one atomic undoable transaction per gesture; persists/survives save-reopen/recovery; missing source handled visibly and safely.\n- Must use explicit testable data model for clone source/aligned-unaligned/coordinate relationship — not implicit UI-handler state.\n- Reuse tiled stores and coordinate helpers; do not materialize entire canvas/layer for a clone op.\n- Add focused unit tests: source/dest pixel correctness, multi-segment aligned strokes, unaligned restart, transformed layers, selection clipping, masks/edit targets, tile-boundary behavior. Add browser workflow verifying undo/redo + persistence/recovery.\n- Do not regress existing Brush/Eraser behavior.\n\n## Cross-Cutting Acceptance Questions (must answer per feature)\n1. Active edit target (document/layer/mask/channel)?\n2. Coordinate space for pointer/source/selection/layer/document?\n3. Which tiles/bounded region are read, allocated, rendered, written?\n4. Behavior when selection empty, source missing, layer transformed, resource not hydrated, optional backend missing?\n5. Exact command/history boundary for one user action; undo/redo without changing unrelated state?\n6. What is persisted, how reopened, how recovered after interruption?\n\n## Progress to Date (from recent raw source items)\n- **types.ts extended:** Added `Tool = ... | 'cloneStamp'`, `CloneSourceAlignMode = 'aligned' | 'unaligned'`, `CloneSource` interface (x, y, aligned), `CloneStampGesture` interface (kind, points, layerId, target, source, sourceLayerId, destinationWidth/Height, sourceWidth/Height). Added `Gesture` union member `{ kind: 'cloneStamp'; gesture: CloneStampGesture }`.\n- **toolRegistry.ts extended:** Added `'cloneStamp'` to `ToolFamilyId` union; added `ToolDefinition` entry for `cloneStamp` tool with label 'Clone Stamp Tool', accessibleLabel 'Clone Stamp', icon 'clone', needsDocument true, family 'cloneStamp', slot 12, shortcut 'S'.\n- **Icon.tsx extended:** Added 'clone' to `ICON_NAMES` and added clone icon path definition.\n- **raster-engine/src/index.ts extended:** Added `CloneStampOptions` interface (size, hardness, opacity, aligned, sourceX, sourceY) and `paintCloneStamp` function skeleton. The current implementation is a placeholder that returns `emptyPatch()` and needs to be properly implemented with the correct clone stamp logic.\n- **ToolOptionsBar.tsx extended:** Added `cloneAlignMode: 'aligned' | 'unaligned'` prop and `onCloneAlignModeChange` callback to `ToolOptionsBarProps`; removed `brushColor` from props (no longer present in current version). The UI rendering for clone stamp options has not yet been added — the component still only renders brush/eraser options for brush/eraser tools, selection tools, crop, transform, pen, and zoom/busy controls.\n- **Still pending:** Complete `paintCloneStamp` implementation with proper aligned/unaligned source sampling, add clone stamp UI options rendering in ToolOptionsBar, add tool registry entry object, command/history integration, unit tests, persistence/recovery story.\n\n## Notes / Next Steps\n- Continue reading ARCHITECTURE.md, DOCUMENT_MODEL.md, toolRegistry.ts, and related editor/model files to identify the correct extension points for Clone Stamp (command/history, tiled stores, coordinate helpers, brush/raster workflow, selection/mask handling).\n- Identify existing brush/raster workflow and command definitions to reuse for clone sampling + destination writes.\n- Plan explicit clone-source data model and aligned/unaligned semantics before writing code.\n- Keep repository buildable; prefer small complete slice over decorative breadth.\n- **Immediate next reads:** `editor/types.ts` full content (already partially read), existing command definitions in `apps/web/src/commands/`, brush/eraser tool implementations, `core-document` layer/patch APIs, `pixel-storage` tile read/write APIs, `command-history` command contract.\n- **Recent reads (raster-engine/src/index.ts spans 5-16):** Confirmed detailed brush/mask/adjustment implementation patterns: `paintStroke`, `paintMaskStroke`, `adjustBrightness`, `adjustBrightnessPixelStore`, `selectionCoverage`, `readSelectionRegion`, `localDabBounds`, `layerBoundsFromDocumentBounds`, `compositeSourceOver`, `dabCoverage`, `multiplyTransforms`, `scalePathCommand`, `resizeLayer`, `resizeImage`, `estimateLayerResizeBytes`, `validateOutputSize`, `validateLayer`, `validateSelection`, `validateRasterMask`, `validateTransform`, `transformPoint`, `invertTransform`, `unionBounds`, `documentDabBounds`, `makePatchFromRegion`, `makeCoveragePatchFromRegion`, `include`, `emptyPatch`, `emptyCoveragePatch`. These are the reusable primitives for clone stamp sampling + destination writes.\n- **Next planned reads:** `packages/core-document/src/index.ts` (layer/patch APIs, document model), `packages/command-history/src/index.ts` (command contract), `apps/web/src/commands/` (existing command definitions), brush/eraser tool implementations, `pixel-storage` tile read/write APIs.\n- **Recent implementation activity:** Added cloneStamp tool to toolRegistry.ts with icon 'clone' (which was added to Icon.tsx). Started implementing `paintCloneStamp` in raster-engine/src/index.ts with `CloneStampOptions` interface including `sourceX`/`sourceY` fields and `aligned` boolean. The function skeleton validates inputs and sets up inverse transforms but the core sampling/writing logic is not yet complete — it currently returns `emptyPatch()` as placeholder. The implementation needs to: convert source position from document space to source layer local space, interpolate points into dabs with source positions calculated per dab (aligned offset from first point vs unaligned constant source), read source pixels, apply falloff/opacity/pressure/selection coverage, write to destination using source-over compositing, track changed bounds, return patch. Also updated ToolOptionsBar.tsx props to include `cloneAlignMode` and `onCloneAlignModeChange`, but the actual clone stamp UI rendering block still needs to be added to the component's JSX.","content_hash":"c00d3ac594ec02e0e6b3a44978a6f16458402b7744381444c0b619d37c806aee","base_capsule_hash":"9bbfb9c54e8dfdf7b07f7e603cd00cd6e710b3b7424fe210c4e4a5b4b43abcb0","source_refs":["replay_evt_000068::span::000000","replay_evt_000068::span::000001","replay_evt_000068::span::000002","replay_evt_000069","replay_evt_000070::span::000000","replay_evt_000070::span::000001","replay_evt_000070::span::000002","replay_evt_000070::span::000003","replay_evt_000071","replay_evt_000072","replay_evt_000073","replay_evt_000074::span::000000","replay_evt_000074::span::000001","replay_evt_000074::span::000002","replay_evt_000074::span::000003"],"covered_end_sequence":74}
