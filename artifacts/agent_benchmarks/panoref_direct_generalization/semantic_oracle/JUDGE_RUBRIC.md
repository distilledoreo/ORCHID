# Frozen PanoRef semantic rubric

This deterministic content-coverage oracle was created before any new direct runs. It checks durable intent, constraints, blockers, and validation evidence from the authoritative coding transcript. A missing term is a conservative coverage miss, not a complete human semantic judgment.
